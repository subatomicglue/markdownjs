import './default_rules.js';

const MarkdownDefaultRules = globalThis.MarkdownDefaultRules;

export const { defaultRules, INLINE_CHILDREN, BLOCK_CHILDREN } = MarkdownDefaultRules;
export default MarkdownDefaultRules;
