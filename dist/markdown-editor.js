/* Generated by npm run build. Do not edit directly. */
(function markdownJsBrowserBundle() {
'use strict';

// default_rules.js
'use strict';

(function installMarkdownDefaultRules(global) {

const INLINE_CHILDREN = [
  'inlineColor',
  'inlineHtml',
  'inlineImage',
  'inlineLink',
  'inlineAutoLink',
  'inlineVariable',
  'inlineStrong',
  'inlineUnderline',
  'inlineEmphasis',
  'inlineCode',
  'inlineLineBreak',
  'inlineText'
];

const BLOCK_CHILDREN = [
  'blankLines',
  'tocDirective',
  'heading',
  'fencedBox',
  'fencedCode',
  'horizontalRule',
  'unorderedList',
  'orderedList',
  'blockquote',
  'invisibleBlockquote',
  'table',
  'paragraph'
];

const FENCE_VARIANTS = [
  { marker: '---', label: 'Box', tag: 'div', className: 'fence-box fence-box--bordered' },
  { marker: '+--', label: 'Box left', tag: 'div', className: 'fence-box fence-box--bordered' },
  { marker: '-+-', label: 'Box center', tag: 'div', className: 'fence-box fence-box--center' },
  { marker: '--+', label: 'Box right', tag: 'div', className: 'fence-box fence-box--right' },
  { marker: '===', label: 'Invisible box', tag: 'div', className: 'fence-box fence-box--plain' },
  { marker: '+==', label: 'Invisible box left', tag: 'div', className: 'fence-box fence-box--plain' },
  { marker: '=+=', label: 'Invisible box center', tag: 'div', className: 'fence-box fence-box--plain-center' },
  { marker: '==+', label: 'Invisible box right', tag: 'div', className: 'fence-box fence-box--plain-right' },
  { marker: '>>>', label: 'Quote', tag: 'blockquote', className: 'fence-quote' },
  { marker: '}}}', label: 'Invisible quote', tag: 'blockquote', className: 'fence-quote fence-quote--invisible' }
];
const FENCE_MARKER_PATTERN = FENCE_VARIANTS
  .map(({ marker }) => marker.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'))
  .join('|');

// Parsing starts at the document rule and follows its child rule names.
const defaultRules = {
  document: {
    parse: 'block',
    regex: null,
    children: BLOCK_CHILDREN
  },
  blankLines: {
    parse: 'block',
    regex: /(?:[ \t]*\n)+/,
    captures: {},
    children: [],
    trimContent: false
  },
  tocDirective: {
    parse: 'block',
    regex: /<!--[ \t]*(toc|toc-all)[ \t]*-->[ \t]*(?:\n|$)/,
    captures: { command: 1 },
    contentGroup: 1,
    trimContent: true,
    children: []
  },
  heading: {
    parse: 'block',
    regex: /(#{1,6})[ \t]+(.+?)(?:\n|$)/,
    captures: { depth: 1, text: 2 },
    contentGroup: 2,
    trimContent: true,
    children: INLINE_CHILDREN,
    editor: {
      label: 'Heading',
      behavior: 'text',
      enter: 'splitText',
      variants: [
        { label: 'Heading 1', tag: 'h1' }, { label: 'Heading 2', tag: 'h2' },
        { label: 'Heading 3', tag: 'h3' }, { label: 'Heading 4', tag: 'h4' },
        { label: 'Heading 5', tag: 'h5' }, { label: 'Heading 6', tag: 'h6' }
      ]
    }
  },
  fencedCode: {
    parse: 'block',
    regex: /```([^\n]*)\n([\s\S]*?)\n```[ \t]*(?:\n|$)/,
    captures: { language: 1 },
    contentGroup: 2,
    children: ['inlineStrong', 'inlineUnderline', 'inlineEmphasis', 'inlineLineBreak', 'inlineText'],
    html: {
      template: '<div class="pre-container pre-coloring"><div class="pre-container-scroll-wrapper"><pre><code>${escapedContent}</code></pre></div></div>'
    },
    editor: { label: 'Code block', tag: 'pre', behavior: 'code', enter: 'codeNewline' }
  },
  horizontalRule: {
    parse: 'block',
    regex: /-{6,}[ \t]*(?:\n|$)/,
    children: [],
    html: { template: '<hr>' }
  },
  fencedBox: {
    parse: 'block',
    regex: new RegExp(`^(${FENCE_MARKER_PATTERN})([A-Za-z]*)[ \\t]*\\n([\\s\\S]*?)\\n\\1[ \\t]*(?:\\n|$)`, 'm'),
    captures: { marker: 1, label: 2 },
    contentGroup: 3,
    childContentGroup: 3,
    children: BLOCK_CHILDREN,
    allowEmptyChildren: true,
    editor: {
      label: 'Fence box',
      behavior: 'container',
      variants: FENCE_VARIANTS
    }
  },
  unorderedList: {
    parse: 'block',
    regex: /(?:[ \t]*[-*+] [^\n]*(?:\n|$))+/, 
    children: ['listItem'],
    nestByIndent: true,
    editor: {
      label: 'Bulleted list', command: 'list', behavior: 'list', enter: 'splitList', shortcut: 'Mod+-'
    }
  },
  listItem: {
    parse: 'block',
    regex: /([ \t]*)[-*+] ([^\n]*)(?:\n|$)/,
    captures: { indent: 1, text: 2 },
    contentGroup: 2,
    trimContent: true,
    children: INLINE_CHILDREN
  },
  orderedList: {
    parse: 'block',
    regex: /(?:[ \t]*(?:[0-9]{1,2}|[A-Za-z]{1,2}|[IVXLCDMivxlcdm]+)\.[ \t]+[^\n]*(?:\n|$))+/,
    children: ['orderedListItem'],
    nestByIndent: true,
    editor: {
      label: 'Numbered list', behavior: 'list', enter: 'splitList'
    }
  },
  orderedListItem: {
    parse: 'block',
    regex: /([ \t]*)((?:[0-9]{1,2}|[A-Za-z]{1,2}|[IVXLCDMivxlcdm]+))\.[ \t]+([^\n]*)(?:\n|$)/,
    captures: { indent: 1, marker: 2, text: 3 },
    contentGroup: 3,
    trimContent: true,
    children: INLINE_CHILDREN
  },
  blockquote: {
    parse: 'block',
    regex: /(?:>+(?=[ \t]|\n|$)[^\n]*(?:\n|$))+/, 
    transformContent: content => content.replace(/^>[ \t]?/gm, ''),
    children: BLOCK_CHILDREN,
    editor: {
      label: 'Block quote',
      behavior: 'quote',
      enter: 'exitQuote',
      marker: '>',
      variants: [1].map(depth => ({
        label: 'Quote',
        tag: 'blockquote',
        value: `blockquote:${depth}`,
        depth
      }))
    }
  },
  invisibleBlockquote: {
    parse: 'block',
    regex: /(?:}+(?=[ \t]|\n|$)[^\n]*(?:\n|$))+/, 
    transformContent: content => content.replace(/^}[ \t]?/gm, ''),
    children: BLOCK_CHILDREN,
    editor: {
      label: 'Invisible quote',
      behavior: 'quote',
      enter: 'exitQuote',
      className: 'invisible-quote',
      marker: '}',
      variants: [{
        label: 'Invisible quote',
        tag: 'blockquote',
        value: 'invisiblequote:1',
        depth: 1
      }]
    }
  },
  table: {
    parse: 'block',
    regex: /(?:\|[^\n]*\|\n?){2,}/,
    children: ['tableRow'],
    editor: { label: 'Table', tag: 'table', behavior: 'table', enter: 'nextTableCell', lineBreak: false }
  },
  tableRow: {
    parse: 'block',
    regex: /\|[^\n]*\|[ \t]*(?:\n|$)/,
    trimContent: true,
    children: ['tableDivider', 'tableCell']
  },
  tableCell: {
    parse: 'block',
    regex: /([^|\n]+)/,
    contentGroup: 1,
    trimContent: true,
    children: INLINE_CHILDREN
  },
  tableDivider: {
    parse: 'block',
    regex: /[ \t]*\|[ \t]*/,
    children: []
  },
  paragraph: {
    parse: 'block',
    regex: /(?:[^\n]|(?:\n(?!\n|#{1,6}[ \t]|```|-{6,}[ \t]*(?:\n|$)|---|<!--[ \t]*toc(?:-all)?[ \t]*-->|[ \t]*[-*+] |[ \t]*(?:[0-9]{1,2}|[A-Za-z]{1,2}|[IVXLCDMivxlcdm]+)\. |>+(?:[ \t]|\n|$)|}+(?:[ \t]|\n|$)|\|))){1,}(?:\n|$)/,
    trimContent: true,
    children: INLINE_CHILDREN,
    editor: { label: 'Paragraph', tag: 'p', behavior: 'text', enter: 'splitText' }
  },
  inlineImage: {
    parse: 'inline',
    regex: /!\[([^\]]*)\]\(([^)]+?)(?:\s+"([^"]+)")?\)/,
    captures: { alt: 1, src: 2, title: 3 },
    captureDefaults: { title: 'alt' },
    children: [],
    editor: {
      label: 'Image', command: 'image', behavior: 'inlineObject', icon: 'Image', toolbarOrder: 60
    }
  },
  inlineHtml: {
    parse: 'inline',
    regex: /(?:<!--[\s\S]*?-->|<\/?[A-Za-z][A-Za-z0-9:-]*(?:\s+(?:[^"'<>]|"[^"]*"|'[^']*')*)?\s*\/?>)/,
    children: []
  },
  inlineColor: {
    parse: 'inline',
    regex: /<span\b[^>]*style\s*=\s*["'][^"']*color\s*:\s*([^;"']+)[^"']*["'][^>]*>(\S(?:[\s\S]*?\S)??)<\/span>/,
    captures: { color: 1, text: 2 },
    contentGroup: 2,
    trimContent: false,
    children: INLINE_CHILDREN,
    editor: {
      label: 'Text color', command: 'color', behavior: 'inlineFormat', control: 'color', toolbarOrder: 35
    }
  },
  inlineLink: {
    parse: 'inline',
    regex: /\[([^\]]+)\]\(([^)]+?)(?:\s+"([^"]+)")?\)/,
    captures: { text: 1, href: 2, title: 3 },
    captureDefaults: { title: 'text' },
    contentGroup: 1,
    children: ['inlineStrong', 'inlineUnderline', 'inlineEmphasis', 'inlineCode', 'inlineImage', 'inlineLineBreak', 'inlineText'],
    editor: {
      label: 'Link', command: 'link', behavior: 'inlineObject', icon: 'Link',
      shortcut: 'Mod+K', toolbarOrder: 50
    }
  },
  inlineVariable: {
    parse: 'inline',
    regex: /\{\{[ \t]*([A-Za-z][\w-]*):([^\s}]+)[ \t]*\}\}/,
    captures: { namespace: 1, id: 2 },
    children: []
  },
  inlineAutoLink: {
    parse: 'inline',
    regex: /https?:\/\/[^\s<]+/,
    children: []
  },
  inlineStrong: {
    parse: 'inline',
    regex: /\*\*(\S(?:[\s\S]*?\S)??)\*\*/,
    balancedDelimiter: '**',
    contentGroup: 1,
    children: ['inlineUnderline', 'inlineEmphasis', 'inlineCode', 'inlineLink', 'inlineImage', 'inlineLineBreak', 'inlineText'],
    editor: {
      label: 'Bold', command: 'bold', behavior: 'inlineFormat', icon: '<strong>B</strong>',
      shortcut: 'Mod+B', toolbarOrder: 10
    }
  },
  inlineUnderline: {
    parse: 'inline',
    regex: /__(\S(?:[\s\S]*?\S)??)(?=__)__(?!_)/,
    contentGroup: 1,
    children: ['inlineStrong', 'inlineEmphasis', 'inlineCode', 'inlineLink', 'inlineImage', 'inlineLineBreak', 'inlineText'],
    editor: {
      label: 'Underline', command: 'underline', behavior: 'inlineFormat', icon: '<u>U</u>',
      shortcut: 'Mod+U', toolbarOrder: 30
    }
  },
  inlineEmphasis: {
    parse: 'inline',
    regex: /(?:(?:(?<!\*)|(?<=\S\*\*))\*(?!\*)(\S(?:[\s\S]*?\S)??)(?=\*)\*(?!\*)|(?<!_)_(?!_)(\S(?:[\s\S]*?\S)??)(?=_)_(?!_))/,
    contentGroup: [1, 2],
    children: ['inlineStrong', 'inlineUnderline', 'inlineCode', 'inlineLink', 'inlineImage', 'inlineLineBreak', 'inlineText'],
    editor: {
      label: 'Italic', command: 'italic', behavior: 'inlineFormat', icon: '<em>I</em>',
      shortcut: 'Mod+I', toolbarOrder: 20
    }
  },
  inlineCode: {
    parse: 'inline',
    regex: /`([^`]+)`/,
    contentGroup: 1,
    children: [],
    editor: {
      label: 'Inline code', command: 'code', behavior: 'inlineFormat', icon: '&lt;/&gt;',
      shortcut: 'Mod+`', toolbarOrder: 40
    }
  },
  inlineLineBreak: {
    parse: 'inline',
    regex: /\n/,
    children: []
  },
  inlineText: {
    parse: 'inline',
    regex: /(?:(?!https?:\/\/)[^<![*_`{\\\n])+|./,
    children: []
  }
};

const markdownRuleSchema = {
  defaultRules,
  INLINE_CHILDREN,
  BLOCK_CHILDREN
};

if (typeof module !== 'undefined' && module.exports) {
  module.exports = markdownRuleSchema;
}
if (global) {
  global.MarkdownDefaultRules = markdownRuleSchema;
}
})(typeof globalThis !== 'undefined' ? globalThis : this);

// markdown.js
'use strict';

// See README.md for architectural details.
const { defaultRules } = typeof require === 'function'
  ? require('./default_rules')
  : globalThis.MarkdownDefaultRules;

const FENCE_RENDER_MAP = Object.fromEntries(
  defaultRules.fencedBox.editor.variants.map(({ marker, tag, className }) => [
    marker,
    { tag: tag || 'div', className: className || '' }
  ])
);

function makeStickyRegex(rx) {
  if (!rx) {
    return null;
  }
  if (typeof rx === 'string') {
    return new RegExp(rx, 'y');
  }
  const flags = rx.flags.includes('y') ? rx.flags : `${rx.flags}y`;
  const uniqueFlags = Array.from(new Set(flags.split(''))).join('');
  return new RegExp(rx.source, uniqueFlags);
}

function cloneRule(rule) {
  return {
    ...rule,
    children: Array.isArray(rule.children) ? [...rule.children] : undefined
  };
}

function normalizeRules(ruleMap) {
  const normalized = {};
  const entries = Object.entries(ruleMap || {});
  entries.forEach(([name, rule]) => {
    const copy = cloneRule(rule);
    copy.name = name;
    if (!copy.parse) {
      throw new Error(`Rule "${name}" is missing a parse type.`);
    }
    copy._regex = makeStickyRegex(copy.regex);
    normalized[name] = copy;
  });
  return normalized;
}

class MarkdownParser {
  constructor(ruleMap) {
    this.rules = normalizeRules(ruleMap || defaultRules);
  }

  parse(markdown, rootRule = 'document') {
    const root = this.rules[rootRule];
    if (!root) {
      throw new Error(`Unknown root rule "${rootRule}".`);
    }
    if (root.regex !== null) {
      throw new Error('Root rule must be a container without a regex.');
    }
    const node = {
      type: rootRule,
      parseType: root.parse,
      raw: markdown,
      content: markdown,
      captures: {},
      children: this.parseSequence(root.children || [], markdown, rootRule)
    };
    return node;
  }

  parseSequence(childNames, text, parentName) {
    if (!childNames || !childNames.length) {
      if (text.length > 0) {
        throw new Error(`Parent "${parentName}" has content but no child rules to parse it.`);
      }
      return [];
    }
    const nodes = [];
    let cursor = 0;
    while (cursor < text.length) {
      let matched = false;
      for (const childName of childNames) {
        const result = this.matchRule(childName, text, cursor);
        if (result) {
          nodes.push(result.node);
          cursor = result.next;
          matched = true;
          break;
        }
      }
      if (!matched) {
        const preview = text.slice(cursor, cursor + 40).replace(/\n/g, '\\n');
        throw new Error(`No rule matched inside "${parentName}" at offset ${cursor}: "${preview}"`);
      }
    }
    return nodes;
  }

  matchBalancedDelimiter(rule, text, cursor) {
    const delimiter = rule.balancedDelimiter;
    const marker = delimiter && delimiter[0];
    const width = delimiter ? delimiter.length : 0;
    if (!marker || width < 2 || delimiter !== marker.repeat(width)) return null;
    if (!text.startsWith(delimiter, cursor) || text[cursor + width] === marker) return null;
    const contentStart = cursor + width;
    if (!text[contentStart] || /\s/.test(text[contentStart])) return null;

    let nested = false;
    let index = contentStart;
    while (index < text.length) {
      if (text[index] !== marker) {
        index += 1;
        continue;
      }
      let backslashes = 0;
      for (let before = index - 1; before >= 0 && text[before] === '\\'; before -= 1) backslashes += 1;
      if (backslashes % 2) {
        index += 1;
        continue;
      }
      let runLength = 1;
      while (text[index + runLength] === marker) runLength += 1;

      if (nested && index > contentStart && !/\s/.test(text[index - 1])) {
        nested = false;
        index += 1;
        runLength -= 1;
        if (runLength >= width) {
          const content = text.slice(contentStart, index);
          const raw = text.slice(cursor, index + width);
          const match = [raw, content];
          match.index = cursor;
          match.input = text;
          return match;
        }
        continue;
      }

      if (!nested && runLength >= width) {
        const content = text.slice(contentStart, index);
        if (!content || /\s$/.test(content)) return null;
        const raw = text.slice(cursor, index + width);
        const match = [raw, content];
        match.index = cursor;
        match.input = text;
        return match;
      }

      if (!nested && runLength === 1 && text[index + 1] && !/\s/.test(text[index + 1])) nested = true;
      index += runLength;
    }
    return null;
  }

  matchRule(ruleName, text, cursor) {
    const rule = this.rules[ruleName];
    if (!rule) {
      throw new Error(`Unknown rule "${ruleName}".`);
    }
    const regex = rule._regex;
    if (!regex) {
      if (rule.parse !== 'block') {
        throw new Error(`Rule "${ruleName}" requires a regex.`);
      }
      const remaining = text.slice(cursor);
      const children = this.parseSequence(rule.children || [], remaining, ruleName);
      return {
        node: {
          type: ruleName,
          parseType: rule.parse,
          raw: remaining,
          content: remaining,
          captures: {},
          children
        },
        next: text.length
      };
    }
    regex.lastIndex = cursor;
    const match = rule.balancedDelimiter
      ? this.matchBalancedDelimiter(rule, text, cursor)
      : regex.exec(text);
    if (!match || match.index !== cursor) {
      return null;
    }
    const raw = match[0];
    if (!raw.length && !rule.allowEmpty) {
      return null;
    }
    const node = {
      type: ruleName,
      parseType: rule.parse,
      raw,
      content: selectContent(match, rule.contentGroup, rule.trimContent),
      captures: extractCaptures(match, rule.captures),
      children: []
    };
    if (typeof rule.transformContent === 'function') {
      node.content = rule.transformContent(node.content, node.captures, raw);
    }
    applyCaptureDefaults(node.captures, rule.captureDefaults);
    let childSource =
      rule.childContentGroup !== undefined
        ? selectContent(match, rule.childContentGroup, rule.childTrim)
        : node.content;
    if (typeof rule.transformChildContent === 'function') {
      childSource = rule.transformChildContent(childSource, node.captures, raw);
    } else if (typeof rule.transformContent === 'function' && rule.childContentGroup !== undefined) {
      childSource = rule.transformContent(childSource, node.captures, raw);
    }
    if (rule.children && rule.children.length) {
      const innerText = childSource || '';
      node.children =
        innerText.length === 0 && rule.allowEmptyChildren
          ? []
          : this.parseSequence(rule.children, innerText, ruleName);
    }
    if (rule.nestByIndent) this.nestListChildren(node);
    return {
      node,
      next: cursor + raw.length
    };
  }

  nestListChildren(node) {
    const flat = node.children || [];
    if (!flat.length) return;
    const width = value => String(value || '').replace(/\t/g, '  ').length;
    const rootIndent = width(flat[0].captures.indent);
    node.children = [];
    const stack = [{ list: node, indent: rootIndent, lastItem: null }];
    flat.forEach(item => {
      const indent = width(item.captures.indent);
      while (stack.length > 1 && indent < stack[stack.length - 1].indent) stack.pop();
      if (indent > stack[stack.length - 1].indent && stack[stack.length - 1].lastItem) {
        const parentItem = stack[stack.length - 1].lastItem;
        const listType = item.type === 'orderedListItem' ? 'orderedList' : 'unorderedList';
        const nested = {
          type: listType,
          parseType: 'block',
          raw: '',
          content: '',
          captures: { marker: item.captures.marker || '' },
          children: []
        };
        parentItem.children.push(nested);
        stack.push({ list: nested, indent, lastItem: null });
      } else if (indent !== stack[stack.length - 1].indent) {
        while (stack.length > 1 && indent !== stack[stack.length - 1].indent) stack.pop();
      }
      const target = stack[stack.length - 1];
      target.list.children.push(item);
      target.lastItem = item;
    });
  }
}

function applyCaptureDefaults(captures, defaults) {
  Object.entries(defaults || {}).forEach(([target, source]) => {
    if (!captures[target]) captures[target] = captures[source] || '';
  });
}

function selectContent(match, groupIndex, trim) {
  let value;
  if (Array.isArray(groupIndex)) {
    for (const index of groupIndex) {
      const candidate = match[index];
      if (typeof candidate === 'string') {
        value = candidate;
        break;
      }
    }
    if (value === undefined) {
      value = '';
    }
  } else if (groupIndex === undefined || groupIndex === null) {
    value = match[0];
  } else {
    value = match[groupIndex] || '';
  }
  if (typeof value !== 'string') {
    value = '';
  }
  return trim ? value.trim() : value;
}

function extractCaptures(match, captureMap) {
  if (!captureMap) {
    return {};
  }
  const output = {};
  Object.entries(captureMap).forEach(([key, index]) => {
    output[key] = match[index] || '';
  });
  return output;
}

function markdownToAST(markdown, ruleOverrides) {
  const rules = ruleOverrides ? ruleOverrides : defaultRules;
  const parser = new MarkdownParser(rules);
  return parser.parse(markdown || '');
}

function escapeHtml(value) {
  return (value || '')
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#39;');
}

function nodeText(node) {
  if (!node) return '';
  if (!node.children || !node.children.length) return node.content || '';
  return node.children.map(nodeText).join('');
}

function headingSlug(value) {
  return String(value || '')
    .trim()
    .replace(/\s/g, '-')
    .replace(/[^a-zA-Z0-9\-_:.]/g, '-');
}

function normalizeFragment(fragment) {
  return String(fragment || '').replace(/\s+/g, '-');
}

function resolveLinkHref(rawHref, options) {
  const href = String(rawHref || '').trim();
  const match = href.match(/^([^?#]*)(\?[^#]*)?(#.*)?$/);
  const path = match ? match[1] : href;
  const query = match && match[2] ? match[2] : '';
  const hash = match && match[3] ? `#${normalizeFragment(match[3].slice(1))}` : '';
  if (/^[a-z][a-z0-9+.-]*:\/\//i.test(path)) {
    return options.linkAbsoluteCallback
      ? options.linkAbsoluteCallback(options.baseUrl || '', href)
      : href;
  }
  if (!path) return `${query}${hash}`;
  const encodedPath = path.split('/').map(segment => encodeURIComponent(segment).replace(/%3A/gi, ':')).join('/');
  if (path.startsWith('/')) return `${encodedPath}${query}${hash}`;
  const relative = `${encodedPath}${query}${hash}`;
  if (options.linkRelativeCallback) return options.linkRelativeCallback(options.baseUrl || '', relative);
  const base = String(options.baseUrl || '').replace(/\/$/, '');
  return base ? `${base}/${relative}` : relative;
}

function renderTocList(headings) {
  if (!headings.length) return '';
  const root = { level: 0, children: [] };
  const stack = [root];
  headings.forEach(heading => {
    while (stack.length > 1 && stack[stack.length - 1].level >= heading.level) stack.pop();
    const item = { ...heading, children: [] };
    stack[stack.length - 1].children.push(item);
    stack.push(item);
  });
  const renderItems = items => `<ul>${items.map(item =>
    `<li><a href="#${escapeHtml(item.slug)}">${escapeHtml(item.text)}</a>${item.children.length ? renderItems(item.children) : ''}</li>`
  ).join('')}</ul>`;
  return renderItems(root.children);
}

function renderFenceNode(context) {
  const { node, indent, renderBlockChildren } = context;
  const marker = node.captures.marker || '---';
  const label = (node.captures.label || '').trim();
  const config = FENCE_RENDER_MAP[marker] || FENCE_RENDER_MAP['---'];
  const inner = renderBlockChildren();
  const classAttr = config.className ? ` class="${config.className}"` : '';
  const labelAttr = label ? ` data-label="${escapeHtml(label)}"` : '';
  const openTag = `${indent}<${config.tag}${classAttr}${labelAttr}>`;
  if (!inner) {
    return `${openTag}</${config.tag}>`;
  }
  return `${openTag}\n${inner}\n${indent}</${config.tag}>`;
}

function renderQuoteNode({ indent, renderBlockChildren }, className = '') {
  const classAttr = className ? ` class="${className}"` : '';
  const inner = renderBlockChildren();
  return `${indent}<blockquote${classAttr}>\n${inner}\n${indent}</blockquote>`;
}

function orderedListType(marker) {
  if (/^\d+$/.test(marker)) return '1';
  if (/^[ivxlcdm]+$/.test(marker)) return 'i';
  if (/^[IVXLCDM]+$/.test(marker)) return 'I';
  if (/^[a-z]+$/.test(marker)) return 'a';
  if (/^[A-Z]+$/.test(marker)) return 'A';
  return '1';
}

function renderListItem({ node, depth, indent, renderChild }) {
  const isInline = child => child.parseType === 'inline' || /^inline/.test(child.type || '');
  const inline = (node.children || [])
    .filter(isInline)
    .map(child => renderChild(child, depth, true)).join('');
  const blocks = (node.children || [])
    .filter(child => !isInline(child))
    .map(child => renderChild(child, depth + 1, false)).join('\n');
  return `${indent}<li>${inline}${blocks ? `\n${blocks}\n${indent}` : ''}</li>`;
}

const defaultRenderers = {
  document: ({ renderBlockChildren }) => renderBlockChildren({ depthOffset: 0 }),
  blankLines: () => '',
  tocDirective: ({ renderer, node }) => renderTocList(renderer.tocHeadings.get(node) || []),
  paragraph: ({ indent, renderInlineChildren }) => `${indent}<p>${renderInlineChildren()}</p>`,
  heading: ({ indent, node, renderInlineChildren, options }) => {
    const depthCapture = node.captures.depth || '';
    const derivedLevel = depthCapture.length ? depthCapture.length : Number(depthCapture) || 1;
    const level = Math.min(6, Math.max(1, derivedLevel));
    const text = nodeText(node);
    const slug = headingSlug(text);
    const idAttribute = options.headingIds === false ? '' : ` id="${escapeHtml(slug)}"`;
    const permalink = options.headingPermalinks === false ? '' :
      `<a title="${escapeHtml(options.headingPermalinkTitle)}" href="#${escapeHtml(slug)}">${options.headingPermalinkIcon}</a>`;
    return `${indent}<h${level}${idAttribute}>${renderInlineChildren()}${permalink}</h${level}>`;
  },
  fencedCode: ({ indent, node }) => {
    const language = node.captures.language
      ? ` class="language-${escapeHtml(node.captures.language.trim())}"`
      : '';
    return `${indent}<pre><code${language}>${escapeHtml(node.content)}</code></pre>`;
  },
  horizontalRule: ({ indent }) => `${indent}<hr>`,
  fencedBox: context => renderFenceNode(context),
  unorderedList: ({ indent, renderBlockChildren }) => {
    const inner = renderBlockChildren();
    return `${indent}<ul>\n${inner}\n${indent}</ul>`;
  },
  listItem: context => renderListItem(context),
  orderedList: ({ indent, node, renderBlockChildren }) => {
    const first = (node.children || []).find(child => child.type === 'orderedListItem');
    const marker = (node.captures && node.captures.marker) || (first && first.captures.marker) || '1';
    const inner = renderBlockChildren();
    return `${indent}<ol type="${orderedListType(marker)}" start="${escapeHtml(marker)}">\n${inner}\n${indent}</ol>`;
  },
  orderedListItem: context => renderListItem(context),
  blockquote: context => renderQuoteNode(context),
  blockquoteLine: ({ indent, renderInlineChildren }) => `${indent}<p>${renderInlineChildren()}</p>`,
  invisibleBlockquote: context => renderQuoteNode(
    context,
    defaultRules.invisibleBlockquote.editor.className || ''
  ),
  invisibleBlockquoteLine: ({ indent, renderInlineChildren }) => `${indent}<p>${renderInlineChildren()}</p>`,
  table: ({ indent, renderBlockChildren }) => {
    const rows = renderBlockChildren();
    return `${indent}<table>\n${rows}\n${indent}</table>`;
  },
  tableRow: ({ indent, renderBlockChildren }) => {
    const cells = renderBlockChildren();
    return `${indent}<tr>\n${cells}\n${indent}</tr>`;
  },
  tableCell: ({ indent, renderInlineChildren }) => `${indent}<td>${renderInlineChildren()}</td>`,
  tableDivider: () => '',
  inlineText: ({ node }) => escapeHtml(node.content),
  inlineColor: ({ node, renderInlineChildren }) =>
    `<span style="color:${escapeHtml((node.captures.color || '').trim())}">${renderInlineChildren()}</span>`,
  inlineHtml: ({ node, options }) => options.allowInlineHtml === false
    ? escapeHtml(node.raw || node.content)
    : (node.raw || node.content),
  inlineStrong: ({ renderInlineChildren }) => `<strong>${renderInlineChildren()}</strong>`,
  inlineUnderline: ({ renderInlineChildren }) => `<u>${renderInlineChildren()}</u>`,
  inlineEmphasis: ({ renderInlineChildren }) => `<em>${renderInlineChildren()}</em>`,
  inlineCode: ({ node }) => `<code>${escapeHtml(node.content)}</code>`,
  inlineVariable: ({ node, options }) => {
    const namespace = node.captures.namespace || '';
    const id = node.captures.id || '';
    const values = options.variables && options.variables[namespace];
    if (values && Object.prototype.hasOwnProperty.call(values, id)) return escapeHtml(values[id]);
    return escapeHtml(node.raw || node.content);
  },
  inlineAutoLink: ({ node }) => `<a href="${escapeHtml(node.content)}">${escapeHtml(node.content)}</a>`,
  inlineLink: ({ node, renderInlineChildren, options }) => {
    const href = resolveLinkHref(node.captures.href || '', options);
    const title = node.captures.title ? node.captures.title.trim() : '';
    const titleAttr = title ? ` title="${escapeHtml(title)}"` : '';
    return `<a href="${escapeHtml(href)}"${titleAttr}>${renderInlineChildren()}</a>`;
  },
  inlineImage: ({ node }) => {
    const src = (node.captures.src || '').trim();
    const alt = (node.captures.alt || '').trim();
    const title = node.captures.title ? node.captures.title.trim() : '';
    const titleAttr = title ? ` title="${escapeHtml(title)}"` : '';
    return `<img src="${escapeHtml(src)}" alt="${escapeHtml(alt)}"${titleAttr} />`;
  },
  inlineLineBreak: () => '<br />',
  __default: ({ renderInlineChildren }) => renderInlineChildren()
};

function resolveTemplateValue(context, path) {
  if (!path) {
    return '';
  }
  return path.split('.').reduce((acc, key) => {
    if (acc === undefined || acc === null) {
      return undefined;
    }
    return acc[key];
  }, context);
}

function renderTemplate(template, context) {
  if (typeof template !== 'string' || !template.length) {
    return '';
  }
  return template.replace(/\$\{([^}]+)\}/g, (_, expression) => {
    const value = resolveTemplateValue(context, expression.trim());
    return value === undefined || value === null ? '' : String(value);
  });
}

function createTemplateRenderer(template) {
  return ({ node, depth, indent, renderInlineChildren, renderBlockChildren }) => {
    const context = {
      content: node.content || '',
      raw: node.raw || '',
      captures: node.captures || {},
      escapedContent: escapeHtml(node.content || ''),
      depth,
      indent
    };

    Object.defineProperties(context, {
      children: {
        configurable: true,
        enumerable: true,
        get() {
          const value =
            node.parseType === 'inline'
              ? renderInlineChildren()
              : renderBlockChildren();
          Object.defineProperty(this, 'children', {
            value,
            writable: false,
            enumerable: true
          });
          return value;
        }
      },
      inlineChildren: {
        configurable: true,
        enumerable: true,
        get() {
          const value = renderInlineChildren();
          Object.defineProperty(this, 'inlineChildren', {
            value,
            writable: false,
            enumerable: true
          });
          return value;
        }
      },
      blockChildren: {
        configurable: true,
        enumerable: true,
        get() {
          const value = renderBlockChildren();
          Object.defineProperty(this, 'blockChildren', {
            value,
            writable: false,
            enumerable: true
          });
          return value;
        }
      }
    });

    return renderTemplate(template, context);
  };
}

function normalizeRendererEntry(entry) {
  if (typeof entry === 'function') {
    return entry;
  }
  if (typeof entry === 'string') {
    return createTemplateRenderer(entry);
  }
  if (entry && typeof entry === 'object') {
    if (typeof entry.render === 'function') {
      return entry.render.bind(entry);
    }
    if (typeof entry.template === 'string') {
      return createTemplateRenderer(entry.template);
    }
  }
  throw new Error('Renderer must be a function, template string, or an object with a render/template property.');
}

function normalizeRendererMap(overrides, rules) {
  const map = {};
  Object.entries(defaultRenderers).forEach(([type, renderer]) => {
    map[type] = normalizeRendererEntry(renderer);
  });
  Object.entries(rules || {}).forEach(([type, rule]) => {
    if (rule && rule.html && typeof rule.html.template === 'string') {
      map[type] = createTemplateRenderer(rule.html.template);
    }
  });
  Object.entries(overrides || {}).forEach(([type, renderer]) => {
    map[type] = normalizeRendererEntry(renderer);
  });
  if (!map.__default) {
    map.__default = ({ renderInlineChildren }) => renderInlineChildren();
  }
  return map;
}

class MarkdownRenderer {
  constructor(rendererMap, options = {}) {
    this.renderers = normalizeRendererMap(rendererMap, options.rules || defaultRules);
    this.options = {
      headingPermalinks: true,
      headingIds: true,
      headingPermalinkTitle: 'Permalink to this heading',
      headingPermalinkIcon: '<span class="copy-icon" role="button" aria-label="Link Icon"></span>',
      variables: {},
      baseUrl: '',
      allowInlineHtml: true,
      ...options
    };
    this.tocHeadings = new WeakMap();
  }

  overrideRenderer(type, renderer) {
    this.renderers[type] = normalizeRendererEntry(renderer);
    return this;
  }

  render(ast) {
    if (!ast) {
      return '';
    }
    this.prepareDocument(ast);
    return this.renderNode(ast, 0);
  }

  prepareDocument(ast) {
    const ordered = [];
    const visit = node => {
      ordered.push(node);
      (node.children || []).filter(child => child.parseType !== 'inline').forEach(visit);
    };
    visit(ast);
    const headings = ordered
      .map((node, index) => ({ node, index }))
      .filter(item => item.node.type === 'heading')
      .map(({ node, index }) => ({
        index,
        level: Math.max(1, Math.min(6, (node.captures.depth || '#').length)),
        text: nodeText(node),
        slug: headingSlug(nodeText(node))
      }));
    ordered.forEach((node, index) => {
      if (node.type !== 'tocDirective') return;
      const command = node.captures.command || node.content;
      this.tocHeadings.set(node, command === 'toc-all' ? headings : headings.filter(heading => heading.index > index));
    });
  }

  renderNode(node, depth, forceInline) {
    if (!node) {
      return '';
    }
    const renderer = this.renderers[node.type] || this.renderers.__default;
    const parseType = node.parseType || 'block';
    const isInline = typeof forceInline === 'boolean' ? forceInline : parseType === 'inline';
    const indent = isInline ? '' : '  '.repeat(depth);
    const renderInlineChildren = () => this.renderChildren(node.children, depth, { inline: true });
    const renderBlockChildren = (options = {}) => {
      const offset = options.depthOffset === undefined ? 1 : options.depthOffset;
      return this.renderChildren(node.children, depth + offset, { inline: false });
    };
    const context = {
      node,
      depth,
      indent,
      isInline,
      renderInlineChildren,
      renderBlockChildren,
      renderChild: (child, childDepth = depth + 1, childInline) =>
        this.renderNode(child, childDepth, childInline),
      escapeHtml,
      options: this.options,
      renderer: this
    };
    const html = renderer ? renderer(context) : '';
    return html || '';
  }

  renderChildren(children, depth, options = {}) {
    const list = Array.isArray(children) ? children : [];
    if (!list.length) {
      return '';
    }
    if (options.inline) {
      return list.map(child => this.renderNode(child, depth, true)).join('');
    }
    return list
      .map(child => this.renderNode(child, depth))
      .filter(html => html !== null && html !== undefined && html !== '')
      .join('\n');
  }
}

const sharedRenderer = new MarkdownRenderer();

function astToHTML(ast, rendererOverrides, options) {
  if (!ast) {
    return '';
  }
  if (rendererOverrides instanceof MarkdownRenderer) {
    return rendererOverrides.render(ast);
  }
  if (rendererOverrides && typeof rendererOverrides === 'object' && !Array.isArray(rendererOverrides)) {
    return new MarkdownRenderer(rendererOverrides, options).render(ast);
  }
  if (options) return new MarkdownRenderer(undefined, options).render(ast);
  return sharedRenderer.render(ast);
}

function overrideRenderer(nodeType, renderer) {
  sharedRenderer.overrideRenderer(nodeType, renderer);
  return sharedRenderer;
}

function variablesFromUserdata(userdata) {
  if (!userdata) return {};
  return {
    user: Object.fromEntries(Object.entries(userdata)
      .filter(([, record]) => record && record.id !== undefined)
      .map(([name, record]) => [String(record.id), name]))
  };
}

function markdownToHtml(markdown, rules, rendererOverrides, renderOptions) {
  let actualRules = rules;
  let actualRenderers = rendererOverrides;
  let options = renderOptions || {};
  if (typeof rules === 'string') {
    actualRules = undefined;
    options = { ...(rendererOverrides || {}), baseUrl: rules };
    actualRenderers = options.renderers;
  } else if (rules && typeof rules === 'object' && !rules.document) {
    options = rules;
    actualRules = rules.rules;
    actualRenderers = rules.renderers;
  }
  options = {
    ...options,
    rules: actualRules || defaultRules,
    variables: { ...variablesFromUserdata(options.userdata), ...(options.variables || {}) }
  };
  options.linkRelativeCallback = options.linkRelativeCallback || options.link_relative_callback;
  options.linkAbsoluteCallback = options.linkAbsoluteCallback || options.link_absolute_callback;
  const ast = markdownToAST(markdown, actualRules);
  return astToHTML(ast, actualRenderers, options);
}

const markdownApi = {
  markdownToAST,
  astToHTML,
  markdownToHtml
};

if (typeof module !== 'undefined' && module.exports) {
  module.exports = markdownApi;
}
if (typeof globalThis !== 'undefined') {
  globalThis.MarkdownJS = markdownApi;
}

// markdown_wysiwyg_editor.js
'use strict';

(function installMarkdownWysiwygEditor(global) {
  const markdownApi = global.MarkdownJS;
  const ruleSchema = global.MarkdownDefaultRules;

  if (!markdownApi || !ruleSchema) {
    throw new Error('Load default_rules.js and markdown.js before markdown_wysiwyg_editor.js.');
  }

  const { markdownToAST, astToHTML } = markdownApi;
  const { defaultRules } = ruleSchema;
  const blockOptions = defaultRules.document.children.flatMap(type => {
    const editor = defaultRules[type] && defaultRules[type].editor;
    if (!editor || type === 'fencedBox' || editor.command) return [];
    const variants = editor.variants || [{ label: editor.label, tag: editor.tag }];
    return variants.map(variant => ({ ...editor, ...variant, type }));
  });
  const FENCES = defaultRules.fencedBox.editor.variants;
  const supportedEditorBehaviors = new Set([
    'container', 'text', 'code', 'quote', 'list', 'table', 'inlineFormat', 'inlineObject'
  ]);
  Object.entries(defaultRules).forEach(([type, rule]) => {
    const behavior = rule.editor && rule.editor.behavior;
    if (behavior && !supportedEditorBehaviors.has(behavior)) {
      throw new Error(`Rule "${type}" uses unsupported editor behavior "${behavior}".`);
    }
  });
  const inlineControls = Object.values(defaultRules)
    .filter(rule => rule.parse === 'inline' && rule.editor && rule.editor.command && (rule.editor.icon || rule.editor.control))
    .map(rule => rule.editor)
    .sort((left, right) => (left.toolbarOrder || 0) - (right.toolbarOrder || 0));
  const listControl = Object.values(defaultRules)
    .map(rule => rule.editor)
    .find(editor => editor && editor.behavior === 'list');
  const shortcutCommands = Object.values(defaultRules).reduce((commands, rule) => {
    const editor = rule.editor;
    if (editor && editor.command && editor.shortcut && editor.shortcut.startsWith('Mod+')) {
      commands[editor.shortcut.slice(4).toLowerCase()] = editor.command;
    }
    return commands;
  }, {});
  const shortcutLabel = shortcut => shortcut ? shortcut.replace('Mod+', 'Ctrl/⌘+') : '';

  const escapeMarkdown = text => text.replace(/([\\`*_[\]])/g, '\\$1');
  const textOf = node => (node.textContent || '').replace(/\u00a0/g, ' ');

  class MarkdownWysiwygEditor extends HTMLElement {
    static get observedAttributes() { return ['markdown']; }

    constructor() {
      super();
      this.attachShadow({ mode: 'open' });
      this._markdown = '';
      this._activeBlock = null;
      this._savedRange = null;
      this._renderShell();
    }

    connectedCallback() {
      if (!this._connected) {
        this._bindEvents();
        this._connected = true;
      }
      const initial = this.hasAttribute('markdown') ? this.getAttribute('markdown') : this._markdown;
      this.value = initial || '';
    }

    attributeChangedCallback(name, oldValue, newValue) {
      if (name === 'markdown' && oldValue !== newValue && this._connected && newValue !== this._markdown) {
        this.value = newValue || '';
      }
    }

    get value() { return this._markdown; }
    set value(markdown) {
      this._markdown = String(markdown || '');
      this._renderMarkdown(this._markdown);
    }

    get markdown() { return this.value; }
    set markdown(value) { this.value = value; }

    registerImageUploadHandler(callback) {
      if (callback !== null && typeof callback !== 'function') {
        throw new TypeError('Image upload handler must be a function or null.');
      }
      this._imageUploadHandler = callback;
      return this;
    }

    registerPasteHandler(callback) {
      if (callback !== null && typeof callback !== 'function') {
        throw new TypeError('Paste handler must be a function or null.');
      }
      this._pasteHandler = callback;
      return this;
    }

    focus(options) {
      const target = this._activeBlock || this._canvas.querySelector('.block');
      (target || this._canvas).focus(options);
    }

    _renderShell() {
      const inlineToolbarButtons = inlineControls.map(control => {
        const shortcut = shortcutLabel(control.shortcut);
        const title = `${control.label}${shortcut ? ` — ${shortcut}` : ''}`;
        if (control.control === 'color') {
          return `<span class="color-control" title="${title}"><span>Color</span><button type="button" data-color-open aria-label="Open text color picker"><span class="color-swatch"></span></button><span class="color-popover" hidden><span class="color-preview"></span><span class="rgb-fields"><label><input type="number" min="0" max="255" value="255" data-color-r />R</label><label><input type="number" min="0" max="255" value="0" data-color-g />G</label><label><input type="number" min="0" max="255" value="0" data-color-b />B</label></span><label class="hex-field"><input type="text" value="#ff0000" pattern="#[0-9A-Fa-f]{3}([0-9A-Fa-f]{3})?" maxlength="7" spellcheck="false" data-color-hex />Hex</label><span class="color-actions"><button type="button" data-command="clear-color" data-color-close>No Color</button><button type="button" data-command="${control.command}" data-color-apply data-color-close value="#ff0000">Apply</button></span></span></span>`;
        }
        return `<button type="button" data-command="${control.command}" title="${title}">${control.icon}</button>`;
      }).join('');
      const listShortcut = shortcutLabel(listControl && listControl.shortcut);
      const listTitle = `${listControl.label}${listShortcut ? ` — ${listShortcut}` : ''}`;
      const blockTypeOptions = blockOptions.map(({ tag, label, value }) =>
        `<option value="block:${value || tag}">${label}</option>`).join('');
      const fenceOptions = FENCES.map(({ marker, label }) =>
        `<option value="fence:${marker}">${label} ${marker}</option>`).join('');
      this.shadowRoot.innerHTML = `
        <style>
          :host { display:block; color:#1f2937; font:14px/1.5 system-ui,sans-serif; }
          .editor { border:1px solid #cbd5e1; border-radius:8px; overflow:hidden; background:white; }
          .toolbar { align-items:center; background:#f8fafc; border-bottom:1px solid #cbd5e1;
            display:flex; flex-wrap:wrap; gap:4px; padding:7px; position:sticky; top:0; z-index:2; }
          button, select { background:white; border:1px solid #cbd5e1; border-radius:5px; color:inherit;
            font:inherit; min-height:30px; padding:3px 8px; }
          button:hover, select:hover { background:#eef2ff; border-color:#818cf8; }
          .color-control { align-items:center; background:white; border:1px solid #cbd5e1; border-radius:5px;
            display:flex; gap:5px; min-height:28px; padding-left:7px; position:relative; }
          .color-control > button { border:0; border-left:1px solid #e2e8f0; border-radius:0; padding:3px 7px; }
          .color-swatch { background:#ff0000; border:1px solid #64748b; display:block; height:18px; width:25px; }
          .color-popover { background:white; border:1px solid #94a3b8; border-radius:7px; box-shadow:0 12px 35px #0f172a44;
            display:grid; gap:10px; left:0; min-width:235px; padding:12px; position:absolute; top:calc(100% + 5px); z-index:20; }
          .color-popover[hidden] { display:none; }
          .color-preview { background:#ff0000; border:1px solid #64748b; border-radius:4px; height:54px; }
          .rgb-fields { display:grid; gap:7px; grid-template-columns:repeat(3,1fr); }
          .rgb-fields label, .hex-field { display:grid; font-size:11px; gap:2px; text-align:center; }
          .rgb-fields input, .hex-field input { border:1px solid #cbd5e1; border-radius:4px; box-sizing:border-box;
            font:13px ui-monospace,monospace; min-width:0; padding:5px; text-align:center; width:100%; }
          .color-actions { display:flex; gap:7px; justify-content:flex-end; }
          button:focus-visible, select:focus-visible, .canvas:focus-visible { outline:2px solid #6366f1; outline-offset:1px; }
          .separator { border-left:1px solid #cbd5e1; height:24px; margin:0 3px; }
          .canvas { min-height:240px; padding:18px 22px 28px 38px; }
          .canvas:empty::before { color:#94a3b8; content:'Start writing…'; }
          .block { border-radius:4px; margin:2px 0; min-height:1.5em; outline:none; position:relative; }
          .block.active { box-shadow:0 0 0 2px #c7d2fe; }
          .block.whole { background:#e0e7ff; box-shadow:0 0 0 2px #6366f1; }
          .handle { color:#94a3b8; cursor:grab; font-weight:bold; left:-25px; opacity:0; position:absolute;
            text-align:center; touch-action:none; top:2px; user-select:none; width:20px; }
          .block.dragging { opacity:.4; }
          .block.drop-before::before, .block.drop-after::after { background:#4f46e5; border-radius:2px;
            content:''; height:3px; left:0; pointer-events:none; position:absolute; right:0; z-index:3; }
          .block.drop-before::before { top:-7px; }
          .block.drop-after::after { bottom:-7px; }
          .delete-block { background:transparent; border:0; color:#94a3b8; cursor:pointer; min-height:22px;
            opacity:0; padding:0; position:absolute; right:-3px; top:-3px; width:22px; }
          .delete-block:hover { background:#fee2e2; color:#b91c1c; }
          .block:hover:not(:has(.block:hover)) > .handle, .block.active > .handle,
          .block:hover:not(:has(.block:hover)) > .delete-block, .block.active > .delete-block { opacity:1; }
          .block > :first-child { margin-top:0; } .block > :last-child { margin-bottom:0; }
          .block p { white-space:pre-wrap; }
          .block + .block { margin-top:10px; }
          .fence-box > .block, .fence-quote > .block { margin-left:0; }
          pre { background:#111827; border-radius:5px; color:#f9fafb; overflow:auto; padding:12px; }
          code { background:#f1f5f9; border-radius:3px; font-family:ui-monospace,monospace; padding:1px 3px; }
          pre code { background:transparent; padding:0; }
          blockquote { border-left:4px solid #cbd5e1; color:#475569; margin-left:0; padding-left:14px; }
          blockquote.invisible-quote, blockquote.fence-quote--invisible {
            border-left:0; color:inherit; padding-left:18px;
          }
          table { border-collapse:collapse; width:100%; } td { border:1px solid #cbd5e1; padding:5px 8px; }
          .fence-box { border:1px solid #cbd5e1; padding:12px; }
          .fence-box--center, .fence-box--plain-center { text-align:center; }
          .fence-box--right, .fence-box--plain-right { text-align:right; }
          .fence-box--plain, .fence-box--plain-center, .fence-box--plain-right { border-color:transparent; }
          dialog { border:0; border-radius:10px; box-shadow:0 18px 55px #0f172a55; max-width:420px; padding:0; width:calc(100% - 28px); }
          dialog::backdrop { background:#0f172a55; }
          .object-form { display:grid; gap:12px; padding:18px; }
          .object-form h2 { font-size:18px; margin:0; }
          .object-form label { display:grid; font-weight:600; gap:4px; }
          .object-form input { border:1px solid #cbd5e1; border-radius:5px; font:inherit; padding:8px; }
          .url-row { display:flex; gap:6px; }
          .url-row input { flex:1; min-width:0; }
          .upload-image { font-size:20px; line-height:1; min-width:38px; }
          .object-actions { display:flex; flex-wrap:wrap; gap:7px; justify-content:flex-end; }
          .object-actions .remove { color:#b91c1c; margin-right:auto; }
          [hidden] { display:none !important; }
          .canvas img[data-inline-object] { border-radius:4px; cursor:grab; display:inline; max-width:100%;
            outline:1px solid transparent; vertical-align:middle; }
          .canvas img[data-inline-object]:hover { outline-color:#818cf8; }
          .canvas a[data-inline-object] { background:#eef2ff; border:1px solid #c7d2fe; border-radius:4px;
            color:#3730a3; cursor:grab; display:inline-block; padding:0 4px; text-decoration:underline; }
          .canvas a[data-inline-object]:hover { background:#e0e7ff; border-color:#818cf8; }
          .canvas .inline-dragging { opacity:.35; }
          @media (max-width:600px) { .canvas { padding-left:32px; } .toolbar { position:static; } }
        </style>
        <div class="editor">
          <div class="toolbar" role="toolbar" aria-label="Markdown formatting">
            ${inlineToolbarButtons}
            <span class="separator"></span>
            <select data-command="block" aria-label="Block type">
              <option value="" disabled>Block type</option>
              <optgroup label="Basic blocks">${blockTypeOptions}</optgroup>
              <optgroup label="Fence blocks">${fenceOptions}</optgroup>
            </select>
            <button type="button" data-command="table-row-add" title="Add table row" hidden>+ Row</button>
            <button type="button" data-command="table-row-remove" title="Delete table row" hidden>− Row</button>
            <button type="button" data-command="table-column-add" title="Add table column" hidden>+ Col</button>
            <button type="button" data-command="table-column-remove" title="Delete table column" hidden>− Col</button>
            <button type="button" data-command="${listControl.command}" title="${listTitle}">${listControl.label}</button>
            <button type="button" data-command="outdent" title="Outdent bullet — Shift+Tab" aria-label="Outdent bullet">⇤</button>
            <button type="button" data-command="indent" title="Indent bullet — Tab" aria-label="Indent bullet">⇥</button>
            <span class="separator"></span>
            <button type="button" data-command="linebreak" title="New line — Shift+Enter" aria-label="New line">+ Line</button>
            <button type="button" data-command="add" title="New block — Enter">+ Block</button>
          </div>
          <div class="canvas" contenteditable="true" role="textbox" aria-multiline="true" spellcheck="true"></div>
        </div>
        <dialog class="object-dialog">
          <form class="object-form">
            <h2>Edit link</h2>
            <label class="text-field">Name<input name="text" type="text" /></label>
            <label class="alt-field">Name<input name="alt" type="text" /></label>
            <label>URL<span class="url-row"><input name="url" type="text" inputmode="url" required /><button class="upload-image" type="button" title="Upload image" aria-label="Upload image">↑</button></span></label>
            <input class="image-file" name="imageFile" type="file" accept="image/*" hidden />
            <label>Tooltip (optional)<input name="title" type="text" /></label>
            <div class="object-actions">
              <button class="remove" type="button">Remove</button>
              <button class="open" type="button">Open</button>
              <button class="cancel" type="button">Cancel</button>
              <button class="apply" type="submit">Apply</button>
            </div>
          </form>
        </dialog>`;
      this._canvas = this.shadowRoot.querySelector('.canvas');
      this._toolbar = this.shadowRoot.querySelector('.toolbar');
      this._objectDialog = this.shadowRoot.querySelector('.object-dialog');
    }

    _bindEvents() {
      this._canvas.addEventListener('pointerdown', event => {
        const block = event.target.closest('.block');
        if (!block) return;
        if (event.target.closest('.delete-block')) {
          event.preventDefault();
          this._selectBlock(block, false);
          this._deleteActiveBlock();
          this._commit();
          return;
        }
        const handle = event.target.closest('.handle');
        this._selectBlock(block, Boolean(handle));
        if (handle) {
          event.preventDefault();
          this._beginBlockDrag(event, block, handle);
        }
      });
      this._canvas.addEventListener('pointermove', event => this._moveBlockDrag(event));
      this._canvas.addEventListener('pointerup', event => this._endBlockDrag(event, true));
      this._canvas.addEventListener('pointercancel', event => this._endBlockDrag(event, false));
      this._canvas.addEventListener('input', () => {
        this._ensureBlockStructure();
        this._commit();
      });
      this._canvas.addEventListener('keydown', event => this._onKeyDown(event));
      this._canvas.addEventListener('copy', event => this._onCopy(event, false));
      this._canvas.addEventListener('cut', event => this._onCopy(event, true));
      this._canvas.addEventListener('paste', event => this._onPaste(event));
      this._canvas.addEventListener('dragstart', event => this._onInlineDragStart(event));
      this._canvas.addEventListener('dragover', event => this._onInlineDragOver(event));
      this._canvas.addEventListener('drop', event => this._onInlineDrop(event));
      this._canvas.addEventListener('dragend', () => this._finishInlineDrag());
      this._canvas.addEventListener('click', event => {
        const object = event.target.closest('a, img');
        if (!object) return;
        event.preventDefault();
        this._openObjectEditor(object.tagName === 'IMG' ? 'image' : 'link', object);
      });
      this._canvas.addEventListener('blur', () => this._saveSelection(), true);
      this._toolbar.addEventListener('pointerdown', event => {
        this._saveSelection();
        if (event.target.closest('button')) event.preventDefault();
      });
      this._toolbar.addEventListener('click', event => {
        const colorOpen = event.target.closest('[data-color-open]');
        if (colorOpen) {
          const popover = this._toolbar.querySelector('.color-popover');
          popover.hidden = !popover.hidden;
          return;
        }
        const control = event.target.closest('[data-command]');
        if (control && control.tagName === 'BUTTON') {
          this._runCommand(control.dataset.command, control.value);
          const popover = this._toolbar.querySelector('.color-popover');
          if (popover && control.matches('[data-color-close]')) popover.hidden = true;
        }
      });
      this._toolbar.addEventListener('input', event => {
        if (event.target.matches('[data-color-r],[data-color-g],[data-color-b],[data-color-hex]')) {
          this._syncColorPanel(event.target);
        }
      });
      this._toolbar.addEventListener('change', event => {
        const control = event.target.closest('[data-command]');
        if (!control) return;
        this._runCommand(control.dataset.command, control.value.trim());
      });
      this.shadowRoot.addEventListener('selectionchange', () => this._saveSelection());
      document.addEventListener('selectionchange', () => this._saveSelection());
      const objectForm = this._objectDialog.querySelector('form');
      objectForm.addEventListener('submit', event => {
        event.preventDefault();
        this._applyObjectEditor();
      });
      objectForm.querySelector('.cancel').addEventListener('click', () => this._objectDialog.close());
      objectForm.querySelector('.remove').addEventListener('click', () => this._removeEditedObject());
      objectForm.querySelector('.open').addEventListener('click', () => {
        const url = objectForm.elements.url.value;
        if (url) global.open(url, '_blank', 'noopener');
      });
      objectForm.querySelector('.upload-image').addEventListener('click', () =>
        objectForm.elements.imageFile.click());
      objectForm.elements.imageFile.addEventListener('change', () => this._uploadSelectedImage());
    }

    _renderMarkdown(markdown) {
      const ast = markdownToAST(markdown);
      this._canvas.replaceChildren();
      ast.children.filter(node => node.type !== 'blankLines').forEach(node => {
        this._canvas.append(this._createBlock(node));
      });
      if (!this._canvas.children.length) this._canvas.append(this._emptyParagraph());
      this._selectBlock(this._canvas.querySelector('.block'), false);
    }

    _createBlock(node) {
      return this._buildBlock(node.type, { node });
    }

    _buildBlock(type, options = {}) {
      const block = document.createElement('div');
      const editor = defaultRules[type] && defaultRules[type].editor || {};
      const behavior = editor.behavior;
      block.className = 'block';
      block.dataset.type = type;
      block.innerHTML = this._blockControls();
      const node = options.node;
      if (node && node.captures && node.captures.label) block.dataset.label = node.captures.label;
      if (behavior === 'quote' && node) {
        const depths = new Set((node.children || []).map(child =>
          ((child.captures && child.captures.depth) || '>').length));
        if (depths.size === 1) block.dataset.quoteDepth = String(Array.from(depths)[0]);
      }

      if (behavior === 'container') {
        const marker = options.marker || (node && node.captures && node.captures.marker) || '---';
        const fence = this._fenceDefinition(marker);
        block.dataset.marker = marker;
        if (options.label) block.dataset.label = options.label;
        const box = document.createElement(fence.tag || 'div');
        box.className = fence.className || '';
        const children = options.children || (node
          ? node.children.filter(child => child.type !== 'blankLines').map(child => this._createBlock(child))
          : []);
        children.forEach(child => box.append(child));
        if (!box.querySelector(':scope > .block')) box.append(this._buildBlock('paragraph'));
        block.append(box);
        return block;
      }

      if (node) {
        block.insertAdjacentHTML('beforeend', astToHTML(node, undefined, {
          allowInlineHtml: false,
          headingIds: false,
          headingPermalinks: false
        }));
        if (behavior === 'quote') {
          const lines = block.querySelectorAll(':scope > blockquote p');
          (node.children || []).forEach((lineNode, index) => {
            if (lines[index]) {
              lines[index].dataset.quoteDepth = String(
                ((lineNode.captures && lineNode.captures.depth) || '>').length
              );
            }
          });
        }
        if (behavior === 'list') {
          const listContent = this._blockContent(block);
          const items = listContent ? listContent.querySelectorAll(':scope > li') : [];
          (node.children || []).forEach((itemNode, index) => {
            const leadingIndent = itemNode.captures && itemNode.captures.indent;
            if (items[index] && leadingIndent) items[index].dataset.indent = leadingIndent;
          });
          this._nestListItemsByIndent(listContent);
        }
        this._decorateInlineObjects(block);
        return block;
      }

      let content;
      if (behavior === 'text') content = document.createElement(options.tag || editor.tag || 'p');
      else if (behavior === 'code') {
        content = document.createElement('pre');
        const code = document.createElement('code');
        code.textContent = options.text || '';
        content.append(code);
      } else if (behavior === 'quote') {
        const depth = Math.max(1, Number(options.depth) || 1);
        block.dataset.quoteDepth = String(depth);
        let outer = null;
        let innermost = null;
        for (let level = 0; level < depth; level += 1) {
          const quote = document.createElement('blockquote');
          const quoteClass = editor.className;
          if (quoteClass) quote.className = quoteClass;
          if (!outer) outer = quote;
          if (innermost) innermost.append(quote);
          innermost = quote;
        }
        content = outer;
        const line = document.createElement('p');
        line.dataset.quoteDepth = String(depth);
        this._appendContent(line, options.content);
        this._ensureParagraphPlaceholder(line);
        innermost.append(line);
      } else if (behavior === 'list') {
        content = document.createElement('ul');
        const items = options.items && options.items.length ? options.items : [options.content];
        items.forEach(itemDefinition => {
          const itemContent = itemDefinition && itemDefinition.content !== undefined
            ? itemDefinition.content
            : itemDefinition;
          const item = document.createElement('li');
          if (itemDefinition && itemDefinition.indent) item.dataset.indent = itemDefinition.indent;
          this._appendContent(item, itemContent);
          this._ensureParagraphPlaceholder(item);
          content.append(item);
        });
        this._nestListItemsByIndent(content);
      } else if (behavior === 'table') {
        content = document.createElement('table');
        const body = document.createElement('tbody');
        const rowCount = Math.max(2, Number(options.rows) || 2);
        const columnCount = Math.max(1, Number(options.columns) || 2);
        for (let rowIndex = 0; rowIndex < rowCount; rowIndex += 1) {
          const row = document.createElement('tr');
          for (let columnIndex = 0; columnIndex < columnCount; columnIndex += 1) {
            const cell = document.createElement('td');
            if (rowIndex === 0 && columnIndex === 0 && options.content) {
              this._appendContent(cell, options.content);
            }
            this._ensureParagraphPlaceholder(cell);
            row.append(cell);
          }
          body.append(row);
        }
        content.append(body);
      } else {
        content = document.createElement('p');
        block.dataset.type = 'paragraph';
      }
      if (!['quote', 'list', 'code', 'table'].includes(behavior)) {
        this._appendContent(content, options.content);
        this._ensureParagraphPlaceholder(content);
      }
      block.append(content);
      this._decorateInlineObjects(block);
      return block;
    }

    _emptyParagraph() {
      return this._buildBlock('paragraph');
    }

    _appendContent(target, content) {
      if (!content) return;
      if (typeof content === 'string') target.textContent = content;
      else target.append(content);
    }

    _blockControls() {
      return '<span class="handle" data-block-control contenteditable="false" title="Select or drag block" aria-label="Select or drag block">⋮⋮</span>' +
        '<button class="delete-block" data-block-control contenteditable="false" type="button" title="Delete block" aria-label="Delete block">×</button>';
    }

    _beginBlockDrag(event, block, handle) {
      if (event.button !== 0) return;
      this._drag = {
        pointerId: event.pointerId,
        source: block,
        handle,
        startX: event.clientX,
        startY: event.clientY,
        container: block.parentElement,
        startIndex: Array.from(block.parentElement.children).indexOf(block),
        target: null,
        after: false,
        moved: false
      };
      handle.setPointerCapture(event.pointerId);
    }

    _moveBlockDrag(event) {
      const drag = this._drag;
      if (!drag || event.pointerId !== drag.pointerId) return;
      const distance = Math.hypot(event.clientX - drag.startX, event.clientY - drag.startY);
      if (!drag.moved && distance < 5) return;
      drag.moved = true;
      event.preventDefault();
      drag.source.classList.add('dragging');
      this._clearDropIndicators();

      const candidates = Array.from(drag.container.querySelectorAll(':scope > .block'))
        .filter(block => block !== drag.source);
      if (!candidates.length) return;
      let target = candidates[candidates.length - 1];
      let after = true;
      for (const candidate of candidates) {
        const rect = candidate.getBoundingClientRect();
        if (event.clientY < rect.top + rect.height / 2) {
          target = candidate;
          after = false;
          break;
        }
      }
      drag.target = target;
      drag.after = after;
      target.classList.add(after ? 'drop-after' : 'drop-before');

      const edge = 45;
      if (event.clientY < edge) global.scrollBy(0, -12);
      else if (event.clientY > global.innerHeight - edge) global.scrollBy(0, 12);
    }

    _endBlockDrag(event, shouldDrop) {
      const drag = this._drag;
      if (!drag || event.pointerId !== drag.pointerId) return;
      if (drag.handle.hasPointerCapture(event.pointerId)) drag.handle.releasePointerCapture(event.pointerId);
      if (shouldDrop && drag.moved && drag.target) {
        if (drag.after) drag.target.after(drag.source);
        else drag.target.before(drag.source);
      }
      drag.source.classList.remove('dragging');
      this._clearDropIndicators();
      const endIndex = Array.from(drag.container.children).indexOf(drag.source);
      this._drag = null;
      if (shouldDrop && drag.moved && endIndex !== drag.startIndex) this._commit();
    }

    _clearDropIndicators() {
      this._canvas.querySelectorAll('.drop-before, .drop-after').forEach(block =>
        block.classList.remove('drop-before', 'drop-after'));
    }

    _ensureBlockStructure() {
      const looseNodes = Array.from(this._canvas.childNodes).filter(node =>
        node.nodeType !== Node.ELEMENT_NODE || !node.classList.contains('block'));
      if (looseNodes.length) {
        const block = this._emptyParagraph();
        const paragraph = block.querySelector('p');
        paragraph.replaceChildren();
        looseNodes.forEach(node => {
          if (node.nodeType === Node.ELEMENT_NODE && /^(DIV|P)$/.test(node.tagName)) {
            while (node.firstChild) paragraph.append(node.firstChild);
            node.remove();
          } else {
            paragraph.append(node);
          }
        });
        this._canvas.append(block);
        this._selectBlock(block, false);
      }
      if (!this._canvas.querySelector('.block')) {
        const block = this._emptyParagraph();
        this._canvas.append(block);
        this._selectBlock(block, false);
      } else if (!this._activeBlock || !this._activeBlock.isConnected) {
        this._selectBlock(this._canvas.querySelector('.block'), false);
      }
    }

    _selectBlock(block, whole) {
      if (!block) return;
      this._canvas.querySelectorAll('.block').forEach(item => item.classList.remove('active', 'whole'));
      this._activeBlock = block;
      block.classList.add('active');
      this._syncToolbarState(block);
      if (whole) {
        block.classList.add('whole');
        const selection = this._getSelection();
        selection.removeAllRanges();
      }
    }

    _syncToolbarState(block) {
      const blockSelect = this._toolbar.querySelector('[data-command="block"]');
      const content = Array.from(block.children).find(child => !child.hasAttribute('data-block-control'));
      const ancestorMarkers = this._ancestorFenceMarkers(block);
      blockSelect.querySelectorAll('option[value^="fence:"]').forEach(option => {
        const marker = option.value.slice('fence:'.length);
        option.disabled = ancestorMarkers.has(marker);
        option.title = option.disabled ? `Already used by an ancestor ${marker} fence` : '';
      });
      const editor = defaultRules[block.dataset.type] && defaultRules[block.dataset.type].editor;
      const tag = content && content.tagName ? content.tagName.toLowerCase() : '';
      const depth = block.dataset.quoteDepth || this._quoteElementDepth(content) || 1;
      const matchingOptions = blockOptions.filter(option => option.type === block.dataset.type);
      const selectedOption = matchingOptions.find(option =>
        (!option.tag || option.tag === tag) && (!option.depth || Number(option.depth) === Number(depth))) ||
        matchingOptions.find(option => !option.tag || option.tag === tag) || matchingOptions[0];
      const blockValue = selectedOption && (selectedOption.value || selectedOption.tag);
      blockSelect.value = block.dataset.type === 'fencedBox'
        ? `fence:${block.dataset.marker || '---'}`
        : (blockValue ? `block:${blockValue}` : '');
      const lineBreakButton = this._toolbar.querySelector('[data-command="linebreak"]');
      lineBreakButton.disabled = editor && editor.lineBreak === false;
      this._toolbar.querySelectorAll('[data-command^="table-"]').forEach(button => {
        button.hidden = !editor || editor.behavior !== 'table';
      });
      this._updateListControls();
    }

    _ancestorFenceMarkers(block) {
      const markers = new Set();
      let ancestor = block.parentElement && block.parentElement.closest('.block[data-type="fencedBox"]');
      while (ancestor) {
        markers.add(ancestor.dataset.marker || '---');
        ancestor = ancestor.parentElement && ancestor.parentElement.closest('.block[data-type="fencedBox"]');
      }
      return markers;
    }

    _getSelection() {
      const shadowSelection = typeof this.shadowRoot.getSelection === 'function'
        ? this.shadowRoot.getSelection()
        : null;
      return shadowSelection || document.getSelection();
    }

    _saveSelection() {
      const selection = this._getSelection();
      if (!selection || !selection.rangeCount) return;
      const range = selection.getRangeAt(0);
      if (this._rangeIsInCanvas(range)) this._savedRange = range.cloneRange();
      this._updateListControls();
    }

    _rangeIsInCanvas(range) {
      if (!range) return false;
      const container = range.commonAncestorContainer;
      const node = container.nodeType === Node.ELEMENT_NODE ? container : container.parentElement;
      return Boolean(node && this._canvas.contains(node));
    }

    _restoreSelection() {
      if (!this._savedRange) return;
      const selection = this._getSelection();
      selection.removeAllRanges();
      selection.addRange(this._savedRange);
    }

    _runCommand(command, value) {
      this._restoreSelection();
      if (command === 'link' || command === 'image') {
        this._openObjectEditor(command);
        return;
      }
      if (command === 'bold' || command === 'italic' || command === 'underline') {
        document.execCommand(command, false);
      }
      if (command === 'color') this._applyInlineColor(value);
      if (command === 'clear-color') this._applyInlineColor(null);
      if (command === 'list') this._toggleListBlock();
      if (command === 'indent') this._indentActiveItem();
      if (command === 'outdent') this._outdentActiveItem();
      if (command === 'block' && value && value.startsWith('block:')) {
        this._formatActiveBlock(value.slice('block:'.length));
      }
      if (command === 'block' && value && value.startsWith('fence:')) {
        this._wrapFence(value.slice('fence:'.length));
      }
      if (command === 'code') this._toggleInlineCode();
      if (command === 'linebreak') this._insertLineBreak();
      if (command === 'add') this._insertEmptyBlock();
      if (command === 'table-row-add') this._addTableRow();
      if (command === 'table-row-remove') this._removeTableRow();
      if (command === 'table-column-add') this._addTableColumn();
      if (command === 'table-column-remove') this._removeTableColumn();
      this._commit();
      this.focus();
    }

    _wrapSelection(tagName) {
      const selection = this._getSelection();
      if (!selection || !selection.rangeCount || selection.isCollapsed) return;
      const range = selection.getRangeAt(0);
      const wrapper = document.createElement(tagName);
      wrapper.append(range.extractContents());
      range.insertNode(wrapper);
      selection.selectAllChildren(wrapper);
    }

    _toggleInlineCode() {
      const selection = this._getSelection();
      if (!selection || !selection.rangeCount) return;
      const range = selection.getRangeAt(0);
      const startElement = range.startContainer.nodeType === Node.ELEMENT_NODE
        ? range.startContainer
        : range.startContainer.parentElement;
      const endElement = range.endContainer.nodeType === Node.ELEMENT_NODE
        ? range.endContainer
        : range.endContainer.parentElement;
      let code = startElement && startElement.closest('code');
      while (code && code.parentElement && code.parentElement.closest('code')) {
        code = code.parentElement.closest('code');
      }
      const selectionIsInsideCode = code && (code === endElement || code.contains(endElement));

      if (selectionIsInsideCode) {
        if (selection.isCollapsed) {
          const offsetRange = document.createRange();
          offsetRange.selectNodeContents(code);
          offsetRange.setEnd(range.startContainer, range.startOffset);
          const offset = offsetRange.toString().length;
          const text = document.createTextNode(code.textContent || '');
          code.replaceWith(text);
          const caret = document.createRange();
          caret.setStart(text, Math.min(offset, text.length));
          caret.collapse(true);
          selection.removeAllRanges();
          selection.addRange(caret);
          this._savedRange = caret.cloneRange();
          return;
        }

        const beforeRange = document.createRange();
        beforeRange.selectNodeContents(code);
        beforeRange.setEnd(range.startContainer, range.startOffset);
        const afterRange = document.createRange();
        afterRange.selectNodeContents(code);
        afterRange.setStart(range.endContainer, range.endOffset);
        const before = beforeRange.toString();
        const selected = range.toString();
        const after = afterRange.toString();
        const fragment = document.createDocumentFragment();
        if (before) {
          const beforeCode = document.createElement('code');
          beforeCode.textContent = before;
          fragment.append(beforeCode);
        }
        const plainText = document.createTextNode(selected);
        fragment.append(plainText);
        if (after) {
          const afterCode = document.createElement('code');
          afterCode.textContent = after;
          fragment.append(afterCode);
        }
        code.replaceWith(fragment);
        const plainRange = document.createRange();
        plainRange.selectNodeContents(plainText);
        selection.removeAllRanges();
        selection.addRange(plainRange);
        this._savedRange = plainRange.cloneRange();
        return;
      }

      if (selection.isCollapsed) return;
      const selected = range.toString();
      const wrapper = document.createElement('code');
      wrapper.textContent = selected;
      range.deleteContents();
      range.insertNode(wrapper);
      const codeRange = document.createRange();
      codeRange.selectNodeContents(wrapper);
      selection.removeAllRanges();
      selection.addRange(codeRange);
      this._savedRange = codeRange.cloneRange();
    }

    _applyInlineColor(color) {
      const selection = this._getSelection();
      if (!selection || !selection.rangeCount || selection.isCollapsed) return false;
      const range = selection.getRangeAt(0);
      if (!this._rangeIsInCanvas(range)) return false;
      const fragment = range.extractContents();
      const boundaryWhitespace = this._trimColorBoundaryWhitespace(fragment);
      if (!(fragment.textContent || '').trim()) {
        range.insertNode(document.createTextNode(boundaryWhitespace.before + boundaryWhitespace.after));
        return false;
      }
      Array.from(fragment.querySelectorAll('span[style*="color"],font[color]')).reverse().forEach(element => {
        element.replaceWith(...element.childNodes);
      });
      const wrapper = document.createElement('span');
      if (color) wrapper.style.color = color;
      else wrapper.dataset.clearColor = '';
      wrapper.append(fragment);
      range.insertNode(wrapper);
      let colorAncestor = wrapper.parentElement && wrapper.parentElement.closest('span[style*="color"],font[color]');
      while (colorAncestor && this._canvas.contains(colorAncestor)) {
        while (wrapper.parentNode !== colorAncestor) this._splitInlineAncestorAround(wrapper);
        this._splitInlineAncestorAround(wrapper);
        colorAncestor = wrapper.parentElement && wrapper.parentElement.closest('span[style*="color"],font[color]');
      }
      if (boundaryWhitespace.before) wrapper.before(document.createTextNode(boundaryWhitespace.before));
      if (boundaryWhitespace.after) wrapper.after(document.createTextNode(boundaryWhitespace.after));
      let selectedNodes = Array.from(wrapper.childNodes);
      if (!color) {
        wrapper.replaceWith(...selectedNodes);
        this._normalizeInlineColors();
      } else wrapper = this._normalizeInlineColors(wrapper);
      if (color) selectedNodes = Array.from(wrapper.childNodes);
      const selectedRange = document.createRange();
      if (color) selectedRange.selectNodeContents(wrapper);
      else if (selectedNodes.length) {
        selectedRange.setStartBefore(selectedNodes[0]);
        selectedRange.setEndAfter(selectedNodes[selectedNodes.length - 1]);
      } else return false;
      selection.removeAllRanges();
      selection.addRange(selectedRange);
      this._savedRange = selectedRange.cloneRange();
      return true;
    }

    _normalizeInlineColors(preferred = null) {
      const selector = 'span[style*="color"],font[color]';
      const colorValue = element => element.tagName === 'FONT'
        ? element.getAttribute('color') || ''
        : element.style.color || '';
      Array.from(this._canvas.querySelectorAll(selector)).reverse().forEach(element => {
        if (!(element.textContent || '') && !element.querySelector('img,br')) element.remove();
      });
      Array.from(this._canvas.querySelectorAll(selector)).forEach(element => {
        if (!element.isConnected) return;
        const previous = element.previousSibling;
        if (previous && previous.nodeType === Node.ELEMENT_NODE && previous.matches(selector)
            && colorValue(previous) === colorValue(element)) {
          while (element.firstChild) previous.append(element.firstChild);
          if (preferred === element) preferred = previous;
          element.remove();
        }
      });
      this._canvas.normalize();
      return preferred;
    }

    _trimColorBoundaryWhitespace(fragment) {
      const textNodes = [];
      const walker = document.createTreeWalker(fragment, NodeFilter.SHOW_TEXT);
      let current;
      while ((current = walker.nextNode())) textNodes.push(current);
      let before = '';
      let after = '';
      for (const node of textNodes) {
        if (!node.isConnected && !fragment.contains(node)) continue;
        const match = (node.nodeValue || '').match(/^\s+/);
        if (!match) break;
        before += match[0];
        node.nodeValue = node.nodeValue.slice(match[0].length);
        if (node.nodeValue) break;
      }
      for (const node of textNodes.slice().reverse()) {
        if (!fragment.contains(node)) continue;
        const match = (node.nodeValue || '').match(/\s+$/);
        if (!match) break;
        after = match[0] + after;
        node.nodeValue = node.nodeValue.slice(0, -match[0].length);
        if (node.nodeValue) break;
      }
      fragment.querySelectorAll('*').forEach(element => {
        if (!element.textContent && !element.querySelector('img,br')) element.remove();
      });
      return { before, after };
    }

    _syncColorPanel(source) {
      const clamp = value => Math.max(0, Math.min(255, Number(value) || 0));
      const hexInput = this._toolbar.querySelector('[data-color-hex]');
      let hex;
      if (source === hexInput) {
        const value = hexInput.value.trim();
        if (!/^#[0-9a-f]{3}(?:[0-9a-f]{3})?$/i.test(value)) return;
        hex = value.length === 4
          ? `#${value.slice(1).split('').map(character => character.repeat(2)).join('')}`
          : value;
        this._toolbar.querySelector('[data-color-r]').value = parseInt(hex.slice(1, 3), 16);
        this._toolbar.querySelector('[data-color-g]').value = parseInt(hex.slice(3, 5), 16);
        this._toolbar.querySelector('[data-color-b]').value = parseInt(hex.slice(5, 7), 16);
      } else {
        const red = clamp(this._toolbar.querySelector('[data-color-r]').value);
        const green = clamp(this._toolbar.querySelector('[data-color-g]').value);
        const blue = clamp(this._toolbar.querySelector('[data-color-b]').value);
        hex = `#${[red, green, blue].map(value => value.toString(16).padStart(2, '0')).join('')}`;
        hexInput.value = hex;
      }
      this._toolbar.querySelector('[data-color-apply]').value = hex;
      this._toolbar.querySelector('.color-preview').style.backgroundColor = hex;
      this._toolbar.querySelector('.color-swatch').style.backgroundColor = hex;
    }

    _splitInlineAncestorAround(node) {
      const parent = node.parentNode;
      if (!parent || parent === this._canvas) return;
      const before = parent.cloneNode(false);
      const after = parent.cloneNode(false);
      while (parent.firstChild && parent.firstChild !== node) before.append(parent.firstChild);
      while (node.nextSibling) after.append(node.nextSibling);
      const replacements = [];
      if (before.childNodes.length) replacements.push(before);
      replacements.push(node);
      if (after.childNodes.length) replacements.push(after);
      parent.replaceWith(...replacements);
    }

    _openObjectEditor(kind, existingObject = null) {
      const form = this._objectDialog.querySelector('form');
      form.querySelectorAll('[data-inline-object]').forEach(object => object.remove());
      const selection = this._getSelection();
      const currentRange = selection && selection.rangeCount ? selection.getRangeAt(0) : null;
      this._objectInsertionRange = this._rangeIsInCanvas(currentRange)
        ? currentRange.cloneRange()
        : (this._rangeIsInCanvas(this._savedRange) ? this._savedRange.cloneRange() : null);
      const selectedText = this._rangeIsInCanvas(currentRange) && !selection.isCollapsed
        ? selection.toString()
        : '';
      if (!existingObject) {
        const context = this._selectionContext();
        const start = context && context.startElement;
        existingObject = start && start.closest(kind === 'image' ? 'img' : 'a');
      }
      this._editedObject = existingObject || null;
      this._editedObjectKind = kind;
      this._editedObjectText = existingObject && existingObject.tagName === 'A'
        ? existingObject.textContent
        : '';
      const isImage = kind === 'image';
      form.querySelector('h2').textContent = `${existingObject ? 'Edit' : 'Insert'} ${kind}`;
      form.querySelector('.text-field').hidden = isImage;
      form.querySelector('.alt-field').hidden = !isImage;
      form.querySelector('.remove').hidden = !existingObject;
      form.querySelector('.open').hidden = !existingObject;
      form.querySelector('.upload-image').hidden = !isImage;
      form.querySelector('.upload-image').disabled = !this._imageUploadHandler;
      form.elements.imageFile.value = '';
      form.elements.text.value = !isImage
        ? (existingObject ? existingObject.textContent : selectedText)
        : '';
      form.elements.alt.value = isImage && existingObject ? existingObject.getAttribute('alt') || '' : '';
      form.elements.url.value = existingObject
        ? existingObject.getAttribute(isImage ? 'src' : 'href') || ''
        : '';
      form.elements.title.value = existingObject ? existingObject.getAttribute('title') || '' : '';
      this._objectDialog.showModal();
      queueMicrotask(() => (form.elements.url.value ? form.elements.url.select() : form.elements.url.focus()));
    }

    async _uploadSelectedImage() {
      const form = this._objectDialog.querySelector('form');
      const file = form.elements.imageFile.files && form.elements.imageFile.files[0];
      if (!file || !this._imageUploadHandler) return;
      const button = form.querySelector('.upload-image');
      const previousLabel = button.textContent;
      button.disabled = true;
      button.textContent = '…';
      form.elements.url.setCustomValidity('');
      try {
        const url = await this._imageUploadHandler(file);
        if (typeof url !== 'string' || !url.trim()) {
          throw new Error('Image upload handler did not return a URL.');
        }
        form.elements.url.value = url.trim();
        if (!form.elements.alt.value) {
          form.elements.alt.value = file.name.replace(/\.[^.]+$/, '');
        }
      } catch (error) {
        form.elements.url.setCustomValidity(error && error.message ? error.message : 'Image upload failed.');
        form.elements.url.reportValidity();
      } finally {
        button.textContent = previousLabel;
        button.disabled = false;
      }
    }

    _applyObjectEditor() {
      const form = this._objectDialog.querySelector('form');
      const kind = this._editedObjectKind;
      const isImage = kind === 'image';
      const url = form.elements.url.value.trim();
      if (!url) return;
      let object = this._editedObject;
      if (!object) {
        object = document.createElement(isImage ? 'img' : 'a');
        const range = this._usableInsertionRange();
        if (!range) return;
        range.deleteContents();
        range.insertNode(object);
      }
      if (isImage) {
        object.setAttribute('src', url);
        object.setAttribute('alt', form.elements.alt.value);
      } else {
        object.setAttribute('href', url);
        const text = form.elements.text.value || url;
        if (!this._editedObject || text !== this._editedObjectText) object.textContent = text;
      }
      this._decorateInlineObjects(object.parentElement || this._activeBlock);
      const defaultTitle = isImage ? object.getAttribute('alt') || '' : object.textContent || '';
      object.setAttribute('title', form.elements.title.value.trim() || defaultTitle);
      this._objectDialog.close();
      this._setCaretAfter(object);
      this._commit();
    }

    _decorateInlineObjects(root) {
      if (!root) return;
      const objects = [];
      if (root.matches && root.matches('a, img')) objects.push(root);
      if (root.querySelectorAll) objects.push(...root.querySelectorAll('a, img'));
      objects.forEach(object => {
        object.draggable = true;
        object.contentEditable = 'false';
        object.tabIndex = 0;
        object.dataset.inlineObject = object.tagName === 'IMG' ? 'image' : 'link';
        object.setAttribute('aria-label', object.tagName === 'IMG'
          ? `Image: ${object.getAttribute('alt') || ''}`
          : `Link: ${object.textContent || object.getAttribute('href') || ''}`);
      });
    }

    _onInlineDragStart(event) {
      const object = event.target.closest('a[data-inline-object], img[data-inline-object]');
      if (!object) return;
      event.stopPropagation();
      this._draggedInlineObject = object;
      object.classList.add('inline-dragging');
      event.dataTransfer.effectAllowed = 'move';
      event.dataTransfer.setData('application/x-markdown-inline', object.dataset.inlineObject);
      event.dataTransfer.setData('text/plain', this._elementToMarkdown(object));
    }

    _onInlineDragOver(event) {
      if (!this._draggedInlineObject) return;
      const block = event.target.closest('.block');
      if (!block || event.target.closest('[data-block-control]')) return;
      event.preventDefault();
      event.dataTransfer.dropEffect = 'move';
    }

    _onInlineDrop(event) {
      const object = this._draggedInlineObject;
      if (!object) return;
      const targetBlock = event.target.closest('.block');
      if (!targetBlock || event.target.closest('[data-block-control]')) return;
      event.preventDefault();
      event.stopPropagation();
      let range = this._caretRangeFromPoint(event.clientX, event.clientY);
      if (!range || range.commonAncestorContainer.getRootNode() !== this.shadowRoot) {
        const content = this._inlineDropContent(targetBlock);
        if (!content) return;
        range = document.createRange();
        range.selectNodeContents(content);
        range.collapse(false);
      }
      const rangeElement = range.startContainer.nodeType === Node.ELEMENT_NODE
        ? range.startContainer
        : range.startContainer.parentElement;
      if (rangeElement && (rangeElement === object || object.contains(rangeElement))) return;
      range.insertNode(object);
      this._selectBlock(targetBlock, false);
      this._setCaretAfter(object);
      this._finishInlineDrag();
      this._commit();
    }

    _caretRangeFromPoint(x, y) {
      if (typeof document.caretPositionFromPoint === 'function') {
        let position;
        try {
          position = document.caretPositionFromPoint(x, y, { shadowRoots: [this.shadowRoot] });
        } catch (_) {
          position = document.caretPositionFromPoint(x, y);
        }
        if (position) {
          const range = document.createRange();
          range.setStart(position.offsetNode, position.offset);
          range.collapse(true);
          return range;
        }
      }
      return typeof document.caretRangeFromPoint === 'function'
        ? document.caretRangeFromPoint(x, y)
        : null;
    }

    _inlineDropContent(block) {
      const content = this._blockContent(block);
      if (!content) return null;
      if (content.tagName === 'UL' || content.tagName === 'OL') return content.querySelector('li');
      if (content.tagName === 'BLOCKQUOTE') return content.querySelector('p') || content;
      if (content.tagName === 'TABLE' || content.tagName === 'PRE') return null;
      return content;
    }

    _finishInlineDrag() {
      if (this._draggedInlineObject) this._draggedInlineObject.classList.remove('inline-dragging');
      this._draggedInlineObject = null;
    }

    _removeEditedObject() {
      const object = this._editedObject;
      if (!object) return;
      let caretTarget = null;
      if (object.tagName === 'A') {
        const fragment = document.createDocumentFragment();
        while (object.firstChild) {
          caretTarget = object.firstChild;
          fragment.append(object.firstChild);
        }
        object.replaceWith(fragment);
      } else {
        const placeholder = document.createTextNode('');
        object.replaceWith(placeholder);
        caretTarget = placeholder;
      }
      this._objectDialog.close();
      if (caretTarget) this._setCaretAfter(caretTarget);
      this._commit();
    }

    _usableInsertionRange() {
      if (this._rangeIsInCanvas(this._objectInsertionRange)) {
        return this._objectInsertionRange.cloneRange();
      }
      if (this._rangeIsInCanvas(this._savedRange)) {
        return this._savedRange.cloneRange();
      }
      const content = this._activeBlock && this._blockContent(this._activeBlock);
      if (!content) return null;
      const range = document.createRange();
      range.selectNodeContents(content);
      range.collapse(false);
      return range;
    }

    _setCaretAfter(node) {
      const selection = this._getSelection();
      const caret = document.createRange();
      caret.setStartAfter(node);
      caret.collapse(true);
      selection.removeAllRanges();
      selection.addRange(caret);
      this._savedRange = caret.cloneRange();
    }

    _formatActiveBlock(value) {
      const option = blockOptions.find(item => (item.value || item.tag) === value);
      if (!option) return;
      this._convertBlock(this._activeBlock, option.type, {
        tag: option.tag,
        depth: option.depth
      });
    }

    _wrapFence(marker) {
      this._convertBlock(this._activeBlock, 'fencedBox', { marker });
    }

    _convertBlock(source, targetType, options = {}) {
      if (!source || !source.isConnected) source = this._canvas.querySelector('.block');
      if (!source) return null;

      if (targetType === 'fencedBox') {
        const marker = options.marker || '---';
        if (this._ancestorFenceMarkers(source).has(marker)) return source;
        if (source.dataset.type === 'fencedBox') {
          const oldContainer = this._blockContent(source);
          const children = oldContainer
            ? Array.from(oldContainer.querySelectorAll(':scope > .block'))
            : [];
          const replacement = this._buildBlock('fencedBox', {
            marker,
            label: source.dataset.label || '',
            children
          });
          source.replaceWith(replacement);
          this._selectBlock(replacement, false);
          return replacement;
        }
        const parent = source.parentElement;
        const next = source.nextSibling;
        const wrapper = this._buildBlock('fencedBox', { marker, children: [source] });
        parent.insertBefore(wrapper, next);
        this._selectBlock(wrapper, false);
        return wrapper;
      }

      if (source.dataset.type === 'fencedBox') {
        const container = this._blockContent(source);
        const children = container ? Array.from(container.querySelectorAll(':scope > .block')) : [];
        if (!children.length) children.push(this._emptyParagraph());
        source.replaceWith(...children);
        source = children[0];
      }

      if (source.dataset.type === 'table') {
        this._selectBlock(source, false);
        return source;
      }
      if (['unorderedList', 'orderedList'].includes(source.dataset.type)) {
        const list = this._blockContent(source);
        const paragraphs = Array.from(list.querySelectorAll(':scope > li')).map(item =>
          this._buildBlock('paragraph', { content: this._cloneChildren(item) }));
        if (!paragraphs.length) paragraphs.push(this._emptyParagraph());
        source.replaceWith(...paragraphs);
        source = paragraphs[0];
      }

      const content = this._blockContent(source);
      const inlineContent = this._inlineContentForConversion(content);
      const replacement = targetType === 'fencedCode'
        ? this._buildBlock('fencedCode', { text: content ? content.textContent : '' })
        : this._buildBlock(targetType, { tag: options.tag, depth: options.depth, content: inlineContent });
      source.replaceWith(replacement);
      this._selectBlock(replacement, false);
      return replacement;
    }

    _toggleListBlock() {
      let source = this._activeBlock && this._activeBlock.isConnected
        ? this._activeBlock
        : this._canvas.querySelector('.block');
      if (!source || ['fencedBox', 'table'].includes(source.dataset.type)) return;
      if (['unorderedList', 'orderedList'].includes(source.dataset.type)) {
        const list = this._blockContent(source);
        const lines = this._flattenListItems(list);
        const content = document.createDocumentFragment();
        lines.forEach((line, index) => {
          if (index) content.append(document.createElement('br'));
          if (line.indent) content.append(document.createTextNode(line.indent));
          content.append(line.content);
        });
        const paragraph = this._buildBlock('paragraph', { content });
        source.replaceWith(paragraph);
        this._selectBlock(paragraph, false);
        return;
      }
      const content = this._blockContent(source);
      const items = this._splitContentIntoLines(content);
      const list = this._buildBlock('unorderedList', {
        items
      });
      source.replaceWith(list);
      this._selectBlock(list, false);
    }

    _splitContentIntoLines(content) {
      const hasMeaningfulContent = Array.from(content ? content.childNodes : []).some(child => {
        if (child.nodeType === Node.TEXT_NODE) return (child.nodeValue || '').replace(/\u200b/g, '').length > 0;
        return child.nodeType === Node.ELEMENT_NODE && child.tagName !== 'BR';
      });
      if (!hasMeaningfulContent) {
        return [{ content: document.createDocumentFragment(), indent: '' }];
      }
      const lines = [{ content: document.createDocumentFragment(), indent: '' }];
      const startLine = () => lines.push({ content: document.createDocumentFragment(), indent: '' });
      Array.from(content ? content.childNodes : []).forEach(child => {
        if (child.nodeType === Node.ELEMENT_NODE && child.tagName === 'BR') {
          startLine();
          return;
        }
        if (child.nodeType === Node.TEXT_NODE && child.nodeValue.includes('\n')) {
          child.nodeValue.split('\n').forEach((part, index) => {
            if (index) startLine();
            if (part) lines[lines.length - 1].content.append(document.createTextNode(part));
          });
          return;
        }
        lines[lines.length - 1].content.append(child.cloneNode(true));
      });
      lines.forEach(line => {
        const firstText = this._firstTextNode(line.content);
        if (!firstText) return;
        const match = firstText.nodeValue.match(/^[ \t]*/);
        line.indent = match ? match[0] : '';
        firstText.nodeValue = firstText.nodeValue.slice(line.indent.length);
      });
      return lines;
    }

    _firstTextNode(root) {
      const walker = document.createTreeWalker(root, NodeFilter.SHOW_TEXT);
      return walker.nextNode();
    }

    _flattenListItems(list, depth = 0) {
      const lines = [];
      Array.from(list.children).filter(item => item.tagName === 'LI').forEach(item => {
        const content = document.createDocumentFragment();
        Array.from(item.childNodes)
          .filter(child => child.nodeType !== Node.ELEMENT_NODE || !['UL', 'OL'].includes(child.tagName))
          .forEach(child => content.append(child.cloneNode(true)));
        lines.push({
          indent: item.dataset.indent !== undefined ? item.dataset.indent : '  '.repeat(depth),
          content
        });
        Array.from(item.children).filter(child => ['UL', 'OL'].includes(child.tagName)).forEach(nested => {
          lines.push(...this._flattenListItems(nested, depth + 1));
        });
      });
      return lines;
    }

    _nestListItemsByIndent(list) {
      if (!list) return;
      const items = Array.from(list.querySelectorAll(':scope > li'));
      if (items.length < 2) return;
      const columns = indent => Array.from(indent || '').reduce(
        (count, character) => count + (character === '\t' ? 2 : 1), 0);
      const baseColumns = Math.min(...items.map(item => columns(item.dataset.indent)));
      const stack = [list];
      let previousItem = null;
      let currentLevel = 0;

      items.forEach(item => {
        const requestedLevel = Math.max(0, Math.floor((columns(item.dataset.indent) - baseColumns) / 2));
        const level = Math.min(requestedLevel, currentLevel + (previousItem ? 1 : 0));
        while (currentLevel > level) {
          stack.pop();
          currentLevel -= 1;
        }
        while (currentLevel < level && previousItem) {
          let nested = Array.from(previousItem.children).find(child => child.tagName === list.tagName);
          if (!nested) {
            nested = document.createElement(list.tagName.toLowerCase());
            previousItem.append(nested);
          }
          stack.push(nested);
          currentLevel += 1;
        }
        stack[stack.length - 1].append(item);
        previousItem = item;
      });
    }

    _currentListItem() {
      const context = this._selectionContext();
      if (!context || !['unorderedList', 'orderedList'].includes(context.block.dataset.type)) return null;
      return context.startElement.closest('li');
    }

    _updateListControls() {
      if (!this._toolbar) return;
      const indentButton = this._toolbar.querySelector('[data-command="indent"]');
      const outdentButton = this._toolbar.querySelector('[data-command="outdent"]');
      if (!indentButton || !outdentButton) return;
      const quoteLine = this._currentQuoteLine();
      if (quoteLine) {
        const depth = Number(quoteLine.dataset.quoteDepth) || this._quoteLineDepth(quoteLine);
        indentButton.disabled = false;
        outdentButton.disabled = depth <= 1;
        indentButton.title = 'Indent quote — Tab';
        outdentButton.title = 'Outdent quote — Shift+Tab';
        return;
      }
      const item = this._currentListItem();
      indentButton.disabled = !item || !item.previousElementSibling;
      const list = item && item.parentElement;
      outdentButton.disabled = !list || !list.parentElement || list.parentElement.tagName !== 'LI';
      indentButton.title = 'Indent bullet — Tab';
      outdentButton.title = 'Outdent bullet — Shift+Tab';
    }

    _indentActiveItem() {
      return this._currentQuoteLine() ? this._changeQuoteDepth(1) : this._indentListItem();
    }

    _outdentActiveItem() {
      return this._currentQuoteLine() ? this._changeQuoteDepth(-1) : this._outdentListItem();
    }

    _currentQuoteLine() {
      const context = this._selectionContext();
      const editor = context && defaultRules[context.block.dataset.type] &&
        defaultRules[context.block.dataset.type].editor;
      if (!context || !editor || editor.behavior !== 'quote') return null;
      const line = context.startElement.closest('p');
      return line && context.block.contains(line) ? line : null;
    }

    _quoteLineDepth(line) {
      let depth = 0;
      let quote = line && line.parentElement;
      while (quote && quote.tagName === 'BLOCKQUOTE') {
        depth += 1;
        quote = quote.parentElement;
      }
      return Math.max(1, depth);
    }

    _changeQuoteDepth(change) {
      const line = this._currentQuoteLine();
      const block = line && line.closest('.block');
      if (!line || !block) return false;
      const selection = this._getSelection();
      const selectedRange = selection && selection.rangeCount ? selection.getRangeAt(0) : null;
      const selectionBookmark = selectedRange && line.contains(selectedRange.commonAncestorContainer)
        ? {
            startContainer: selectedRange.startContainer,
            startOffset: selectedRange.startOffset,
            endContainer: selectedRange.endContainer,
            endOffset: selectedRange.endOffset
          }
        : null;
      const lines = Array.from(block.querySelectorAll(':scope > blockquote p'));
      const entries = lines.map(item => ({
        line: item,
        depth: Number(item.dataset.quoteDepth) || this._quoteLineDepth(item)
      }));
      const entry = entries.find(item => item.line === line);
      if (!entry) return false;
      const nextDepth = Math.max(1, entry.depth + change);
      if (nextDepth === entry.depth) return false;
      entry.depth = nextDepth;
      entry.line.dataset.quoteDepth = String(nextDepth);
      this._rebuildQuoteLines(block, entries);
      if (selectionBookmark && line.isConnected) {
        const restoredRange = document.createRange();
        restoredRange.setStart(selectionBookmark.startContainer, selectionBookmark.startOffset);
        restoredRange.setEnd(selectionBookmark.endContainer, selectionBookmark.endOffset);
        selection.removeAllRanges();
        selection.addRange(restoredRange);
        this._savedRange = restoredRange.cloneRange();
      }
      const depths = new Set(entries.map(item => item.depth));
      if (depths.size === 1) block.dataset.quoteDepth = String(entries[0].depth);
      else delete block.dataset.quoteDepth;
      this._updateListControls();
      return true;
    }

    _rebuildQuoteLines(block, entries) {
      const oldQuote = this._blockContent(block);
      const quoteClass = defaultRules[block.dataset.type].editor.className || '';
      const stack = [];
      let outer = null;
      entries.forEach(entry => {
        while (stack.length > entry.depth) stack.pop();
        while (stack.length < entry.depth) {
          const quote = document.createElement('blockquote');
          if (quoteClass) quote.className = quoteClass;
          if (stack.length) stack[stack.length - 1].append(quote);
          else outer = quote;
          stack.push(quote);
        }
        stack[entry.depth - 1].append(entry.line);
      });
      if (oldQuote && outer) oldQuote.replaceWith(outer);
    }

    _indentListItem() {
      const item = this._currentListItem();
      const previous = item && item.previousElementSibling;
      if (!item || !previous || previous.tagName !== 'LI') return false;
      const listTag = item.parentElement.tagName;
      let nested = Array.from(previous.children).find(child => child.tagName === listTag);
      if (!nested) {
        nested = document.createElement(listTag.toLowerCase());
        previous.append(nested);
      }
      item.dataset.indent = `${previous.dataset.indent || ''}  `;
      nested.append(item);
      this._updateListControls();
      return true;
    }

    _outdentListItem() {
      const item = this._currentListItem();
      const list = item && item.parentElement;
      const parentItem = list && list.parentElement;
      if (!item || !parentItem || parentItem.tagName !== 'LI') return false;
      item.dataset.indent = parentItem.dataset.indent || '';
      parentItem.after(item);
      if (!list.querySelector(':scope > li')) list.remove();
      this._updateListControls();
      return true;
    }

    _blockContent(block) {
      return Array.from(block.children).find(child => !child.hasAttribute('data-block-control')) || null;
    }

    _cloneChildren(element) {
      const fragment = document.createDocumentFragment();
      if (element) Array.from(element.childNodes).forEach(child => fragment.append(child.cloneNode(true)));
      return fragment;
    }

    _inlineContentForConversion(content) {
      if (!content) return document.createDocumentFragment();
      if (content.tagName === 'BLOCKQUOTE') {
        const line = content.querySelector('p');
        return this._cloneChildren(line);
      }
      if (content.tagName === 'PRE') return document.createTextNode(content.textContent || '');
      return this._cloneChildren(content);
    }

    _quoteElementDepth(content) {
      let depth = 0;
      let current = content;
      while (current && current.tagName === 'BLOCKQUOTE') {
        depth += 1;
        current = Array.from(current.children).find(child => child.tagName === 'BLOCKQUOTE');
      }
      return depth;
    }

    _fenceDefinition(marker) {
      return FENCES.find(fence => fence.marker === marker) || {
        marker,
        tag: 'div',
        className: 'fence-box'
      };
    }

    _insertEmptyBlock(after = this._activeBlock) {
      const block = this._emptyParagraph();
      if (after && after.isConnected) after.after(block); else this._canvas.append(block);
      this._selectBlock(block, false);
      const paragraph = block.querySelector('p');
      const range = document.createRange();
      range.selectNodeContents(paragraph);
      range.collapse(true);
      const selection = this._getSelection();
      selection.removeAllRanges(); selection.addRange(range);
    }

    _deleteActiveBlock() {
      if (!this._activeBlock) return;
      const parent = this._activeBlock.parentElement;
      const next = this._activeBlock.nextElementSibling || this._activeBlock.previousElementSibling;
      this._activeBlock.remove();
      let replacement = next && next.isConnected ? next : null;
      if (parent === this._canvas && !this._canvas.querySelector(':scope > .block')) {
        replacement = this._emptyParagraph();
        this._canvas.append(replacement);
      } else if (parent !== this._canvas && !parent.querySelector(':scope > .block')) {
        replacement = this._emptyParagraph();
        parent.append(replacement);
      }
      this._selectBlock(replacement || parent.closest('.block') || this._canvas.querySelector('.block'), false);
    }

    _onKeyDown(event) {
      const inlineObject = event.target.closest && event.target.closest('[data-inline-object]');
      if (inlineObject && (event.key === 'Enter' || event.key === ' ')) {
        event.preventDefault();
        this._openObjectEditor(inlineObject.dataset.inlineObject, inlineObject);
        return;
      }
      if (inlineObject && (event.key === 'Delete' || event.key === 'Backspace')) {
        event.preventDefault();
        this._editedObject = inlineObject;
        this._removeEditedObject();
        return;
      }
      if ((event.ctrlKey || event.metaKey) && !event.altKey && event.key.toLowerCase() === 'a') {
        event.preventDefault();
        this._selectActiveBlockContents(event.target);
        return;
      }
      if ((event.ctrlKey || event.metaKey) && !event.altKey) {
        const command = shortcutCommands[event.key.toLowerCase()];
        if (command) {
          event.preventDefault();
          this._saveSelection();
          this._runCommand(command);
          return;
        }
      }
      if (event.key === 'Enter' && event.shiftKey && this._insertLineBreak()) {
        event.preventDefault();
        this._commit();
        return;
      }
      if (event.key === 'Enter' && !event.shiftKey && this._handleBlockEnter()) {
        event.preventDefault();
        this._commit();
        return;
      }
      if (event.key === 'Tab' && this._moveToAdjacentTableCell(event.shiftKey ? -1 : 1)) {
        event.preventDefault();
        this._commit();
        return;
      }
      if (event.key === 'Tab' && this._activeBlock &&
          ['unorderedList', 'orderedList', 'blockquote', 'invisibleBlockquote'].includes(this._activeBlock.dataset.type)) {
        event.preventDefault();
        const changed = event.shiftKey ? this._outdentActiveItem() : this._indentActiveItem();
        if (changed) this._commit();
        return;
      }
      if (event.key === 'Escape' && this._activeBlock) {
        event.preventDefault(); this._selectBlock(this._activeBlock, true); return;
      }
      if (this._activeBlock && this._activeBlock.classList.contains('whole')) {
        if (event.key === 'Delete' || event.key === 'Backspace') {
          event.preventDefault(); this._deleteActiveBlock(); this._commit(); return;
        }
        if (event.key === 'ArrowUp' || event.key === 'ArrowDown') {
          event.preventDefault();
          const target = event.key === 'ArrowUp' ? this._activeBlock.previousElementSibling : this._activeBlock.nextElementSibling;
          if (target) this._selectBlock(target, true);
        }
      }
    }

    _selectActiveBlockContents(target) {
      const context = this._selectionContext();
      const targetBlock = target && target.closest ? target.closest('.block') : null;
      const block = context && context.block || targetBlock || this._activeBlock;
      const content = block && this._blockContent(block);
      if (!content) return false;
      this._selectBlock(block, false);
      const range = document.createRange();
      range.selectNodeContents(content);
      const selection = this._getSelection();
      selection.removeAllRanges();
      selection.addRange(range);
      this._savedRange = range.cloneRange();
      return true;
    }

    _insertLineBreak() {
      const context = this._selectionContext();
      if (!context || context.block.dataset.type === 'table') return false;
      if (context.block.dataset.type === 'fencedCode') return this._insertCodeNewline(context);

      const { selection, range } = context;
      range.deleteContents();
      const lineBreak = document.createElement('br');
      const caretAnchor = document.createTextNode('\u200b');
      range.insertNode(caretAnchor);
      range.insertNode(lineBreak);
      const caret = document.createRange();
      caret.setStartAfter(lineBreak);
      caret.collapse(true);
      selection.removeAllRanges();
      selection.addRange(caret);
      this._savedRange = caret.cloneRange();
      return true;
    }

    _handleBlockEnter() {
      const context = this._selectionContext();
      if (!context) return false;
      const type = context.block.dataset.type;
      const enter = defaultRules[type] && defaultRules[type].editor && defaultRules[type].editor.enter;
      if (enter === 'splitText') return this._splitTextBlockAtCursor(context);
      if (enter === 'splitList') return this._splitListItemAtCursor(context);
      if (enter === 'exitQuote') return this._exitBlockquoteAtCursor(context);
      if (enter === 'codeNewline') return this._insertCodeNewline(context);
      if (enter === 'nextTableCell') return this._moveToAdjacentTableCell(1);
      return false;
    }

    _selectionContext() {
      const selection = this._getSelection();
      if (!selection || !selection.rangeCount) return null;
      const range = selection.getRangeAt(0);
      const startElement = range.startContainer.nodeType === Node.ELEMENT_NODE
        ? range.startContainer
        : range.startContainer.parentElement;
      const block = startElement && startElement.closest('.block');
      return block ? { selection, range, startElement, block } : null;
    }

    _splitTextBlockAtCursor(context = this._selectionContext()) {
      if (!context) return false;
      const { selection, startElement, block } = context;
      let { range } = context;
      const content = Array.from(block.children).find(child =>
        !child.hasAttribute('data-block-control') && /^(P|H[1-6])$/.test(child.tagName));
      if (!content || !(content === range.startContainer || content.contains(range.startContainer))) return false;

      if (block.dataset.type === 'paragraph' && this._exitFenceFromEmptyParagraph(block, content, selection)) return true;

      if (!selection.isCollapsed) {
        range.deleteContents();
        range = selection.getRangeAt(0);
      }

      const tailRange = document.createRange();
      tailRange.selectNodeContents(content);
      tailRange.setStart(range.startContainer, range.startOffset);
      const tail = tailRange.extractContents();

      const newBlock = this._emptyParagraph();
      const newParagraph = newBlock.querySelector('p');
      newParagraph.replaceChildren();
      newParagraph.append(tail);
      this._removeEmptyInlineElements(content);
      this._removeEmptyInlineElements(newParagraph);
      this._ensureParagraphPlaceholder(content);
      this._ensureParagraphPlaceholder(newParagraph);
      block.after(newBlock);
      this._selectBlock(newBlock, false);

      const caret = document.createRange();
      caret.selectNodeContents(newParagraph);
      caret.collapse(true);
      selection.removeAllRanges();
      selection.addRange(caret);
      this._savedRange = caret.cloneRange();
      return true;
    }

    _splitListItemAtCursor({ selection, range, startElement, block }) {
      const item = startElement.closest('li');
      const list = item && item.closest('ul,ol');
      if (!item || !list || !block.contains(list)) return false;
      if (!selection.isCollapsed) range.deleteContents();
      if (!(item.textContent || '').length && !item.nextElementSibling) {
        if (list.parentElement && list.parentElement.tagName === 'LI') {
          const parentItem = list.parentElement;
          parentItem.after(item);
          if (!list.querySelector(':scope > li')) list.remove();
          this._ensureParagraphPlaceholder(item);
          this._placeCaretAtStart(item, selection);
          this._updateListControls();
          return true;
        }
        item.remove();
        const paragraphBlock = this._emptyParagraph();
        block.after(paragraphBlock);
        if (!list.querySelector('li')) block.remove();
        this._selectBlock(paragraphBlock, false);
        this._placeCaretAtStart(paragraphBlock.querySelector('p'), selection);
        return true;
      }
      const tailRange = document.createRange();
      tailRange.selectNodeContents(item);
      tailRange.setStart(range.startContainer, range.startOffset);
      const tail = tailRange.extractContents();
      const newItem = document.createElement('li');
      if (item.dataset.indent !== undefined) newItem.dataset.indent = item.dataset.indent;
      newItem.append(tail);
      this._ensureParagraphPlaceholder(item);
      this._ensureParagraphPlaceholder(newItem);
      item.after(newItem);
      this._placeCaretAtStart(newItem, selection);
      return true;
    }

    _exitBlockquoteAtCursor({ selection, range, startElement, block }) {
      const quote = startElement.closest('blockquote');
      if (!quote || !block.contains(quote)) return false;
      let line = startElement.closest('p');
      if (!line) {
        line = document.createElement('p');
        while (quote.firstChild) line.append(quote.firstChild);
        quote.append(line);
      }
      if (!selection.isCollapsed) range.deleteContents();
      const tailRange = document.createRange();
      tailRange.selectNodeContents(line);
      tailRange.setStart(range.startContainer, range.startOffset);
      const tail = tailRange.extractContents();
      const paragraphBlock = this._buildBlock('paragraph', { content: tail });
      const newParagraph = paragraphBlock.querySelector('p');
      this._ensureParagraphPlaceholder(line);
      if ((this._blockContent(block).textContent || '').length) block.after(paragraphBlock);
      else block.replaceWith(paragraphBlock);
      this._selectBlock(paragraphBlock, false);
      this._placeCaretAtStart(newParagraph, selection);
      return true;
    }

    _insertCodeNewline({ selection, range, startElement, block }) {
      const code = startElement.closest('code');
      if (!code || !block.contains(code)) return false;
      range.deleteContents();
      const newline = document.createTextNode('\n');
      range.insertNode(newline);
      const caret = document.createRange();
      caret.setStartAfter(newline);
      caret.collapse(true);
      selection.removeAllRanges();
      selection.addRange(caret);
      this._savedRange = caret.cloneRange();
      return true;
    }

    _moveToAdjacentTableCell(direction) {
      const context = this._selectionContext();
      if (!context || context.block.dataset.type !== 'table') return false;
      const cell = context.startElement.closest('td, th');
      const table = cell && cell.closest('table');
      if (!cell || !table) return false;
      let cells = Array.from(table.querySelectorAll('td, th'));
      let index = cells.indexOf(cell) + direction;
      if (index >= cells.length) {
        const columnCount = cell.parentElement.cells.length;
        const row = table.insertRow();
        for (let column = 0; column < columnCount; column += 1) row.insertCell().append(document.createElement('br'));
        cells = Array.from(table.querySelectorAll('td, th'));
        index = cells.length - columnCount;
      }
      if (index < 0) index = 0;
      this._placeCaretAtStart(cells[index], context.selection);
      return true;
    }

    _currentTableCell() {
      const context = this._selectionContext();
      if (!context || context.block.dataset.type !== 'table') return null;
      return context.startElement.closest('td, th');
    }

    _addTableRow() {
      const table = this._activeBlock && this._activeBlock.querySelector(':scope > table');
      if (!table) return false;
      const currentCell = this._currentTableCell();
      const currentRow = currentCell && currentCell.parentElement;
      const columnCount = (currentRow || table.rows[0]).cells.length;
      const row = document.createElement('tr');
      for (let column = 0; column < columnCount; column += 1) {
        const cell = document.createElement('td');
        cell.append(document.createElement('br'));
        row.append(cell);
      }
      if (currentRow) currentRow.after(row);
      else table.tBodies[0].append(row);
      this._placeCaretAtStart(row.cells[0]);
      return true;
    }

    _removeTableRow() {
      const table = this._activeBlock && this._activeBlock.querySelector(':scope > table');
      const cell = this._currentTableCell();
      const row = cell && cell.parentElement;
      if (!table || !row || table.rows.length <= 2) return false;
      const rowIndex = row.rowIndex;
      const columnIndex = cell.cellIndex;
      row.remove();
      const targetRow = table.rows[Math.min(rowIndex, table.rows.length - 1)];
      this._placeCaretAtStart(targetRow.cells[Math.min(columnIndex, targetRow.cells.length - 1)]);
      return true;
    }

    _addTableColumn() {
      const table = this._activeBlock && this._activeBlock.querySelector(':scope > table');
      if (!table) return false;
      const currentCell = this._currentTableCell();
      const columnIndex = currentCell ? currentCell.cellIndex + 1 : table.rows[0].cells.length;
      Array.from(table.rows).forEach(row => {
        const cell = row.insertCell(columnIndex);
        cell.append(document.createElement('br'));
      });
      this._placeCaretAtStart(table.rows[0].cells[columnIndex]);
      return true;
    }

    _removeTableColumn() {
      const table = this._activeBlock && this._activeBlock.querySelector(':scope > table');
      const cell = this._currentTableCell();
      if (!table || !cell || table.rows[0].cells.length <= 1) return false;
      const columnIndex = cell.cellIndex;
      const rowIndex = cell.parentElement.rowIndex;
      Array.from(table.rows).forEach(row => row.deleteCell(columnIndex));
      const targetColumn = Math.min(columnIndex, table.rows[0].cells.length - 1);
      this._placeCaretAtStart(table.rows[Math.min(rowIndex, table.rows.length - 1)].cells[targetColumn]);
      return true;
    }

    _placeCaretAtStart(element, selection = this._getSelection()) {
      const caret = document.createRange();
      caret.selectNodeContents(element);
      caret.collapse(true);
      selection.removeAllRanges();
      selection.addRange(caret);
      this._savedRange = caret.cloneRange();
    }

    _exitFenceFromEmptyParagraph(block, paragraph, selection) {
      if ((paragraph.textContent || '').length || block.nextElementSibling) return false;
      const container = block.parentElement;
      const fence = container && container.closest('.block[data-type="fencedBox"]');
      if (!fence || fence === block || fence.parentElement === container) return false;

      const siblingBlocks = container.querySelectorAll(':scope > .block');
      if (siblingBlocks.length === 1) container.append(this._emptyParagraph());
      fence.after(block);
      this._selectBlock(block, false);
      this._ensureParagraphPlaceholder(paragraph);

      const caret = document.createRange();
      caret.selectNodeContents(paragraph);
      caret.collapse(true);
      selection.removeAllRanges();
      selection.addRange(caret);
      this._savedRange = caret.cloneRange();
      return true;
    }

    _ensureParagraphPlaceholder(paragraph) {
      const hasText = (paragraph.textContent || '').length > 0;
      const hasContent = Array.from(paragraph.children).some(child => child.tagName !== 'BR');
      if (!hasText && !hasContent) paragraph.replaceChildren(document.createElement('br'));
    }

    _removeEmptyInlineElements(root) {
      root.querySelectorAll('code, strong, em, b, i, u, a').forEach(element => {
        if (!(element.textContent || '').length && !element.querySelector('img, br')) element.remove();
      });
    }

    _onCopy(event, cut) {
      if (!this._activeBlock || !this._activeBlock.classList.contains('whole')) return;
      event.preventDefault();
      const markdown = this._blockToMarkdown(this._activeBlock);
      event.clipboardData.setData('text/plain', markdown);
      event.clipboardData.setData('application/x-markdown-block', markdown);
      if (cut) { this._deleteActiveBlock(); this._commit(); }
    }

    async _onPaste(event) {
      const markdown = event.clipboardData.getData('application/x-markdown-block');
      if (markdown && this._activeBlock && this._activeBlock.classList.contains('whole')) {
        event.preventDefault();
        let cursor = this._activeBlock;
        markdownToAST(markdown).children.filter(node => node.type !== 'blankLines').forEach(node => {
          const block = this._createBlock(node); cursor.after(block); cursor = block;
        });
        this._selectBlock(cursor, true);
        this._commit();
        return;
      }

      const clipboard = event.clipboardData;
      const types = Array.from(clipboard.types || []);
      const files = Array.from(clipboard.files || []);
      const hasRichContent = files.length > 0 || types.some(type => type !== 'text/plain');
      if (!hasRichContent) return;

      event.preventDefault();
      const selection = this._getSelection();
      const range = selection && selection.rangeCount ? selection.getRangeAt(0) : this._savedRange;
      const markdownSelection = this._markdownSelectionForRange(range);
      const paste = {
        types,
        text: clipboard.getData('text/plain'),
        html: clipboard.getData('text/html'),
        files,
        markdown: markdownSelection.markdown,
        markdownPosition: markdownSelection.start,
        markdownSelection: {
          start: markdownSelection.start,
          end: markdownSelection.end
        }
      };

      try {
        if (this._pasteHandler) await this._pasteHandler(paste, this);
      } catch (error) {
        this.dispatchEvent(new CustomEvent('pasteerror', { bubbles: true, detail: { error, paste } }));
      }
    }

    _nodePath(root, node) {
      if (!node || (node !== root && !root.contains(node))) return null;
      const path = [];
      let current = node;
      while (current && current !== root) {
        const parent = current.parentNode;
        if (!parent) return null;
        path.unshift(Array.prototype.indexOf.call(parent.childNodes, current));
        current = parent;
      }
      return current === root ? path : null;
    }

    _nodeAtPath(root, path) {
      return (path || []).reduce((node, index) => node && node.childNodes[index], root);
    }

    _markdownSelectionForRange(range) {
      const fallback = this._markdownFromRoot(this._canvas);
      if (!range || !this._rangeIsInCanvas(range)) {
        return { markdown: fallback, start: fallback.length, end: fallback.length };
      }
      const startPath = this._nodePath(this._canvas, range.startContainer);
      const endPath = this._nodePath(this._canvas, range.endContainer);
      const clone = this._canvas.cloneNode(true);
      const startContainer = this._nodeAtPath(clone, startPath);
      const endContainer = this._nodeAtPath(clone, endPath);
      if (!startContainer || !endContainer) {
        return { markdown: fallback, start: fallback.length, end: fallback.length };
      }

      const nonce = `${Date.now()}${Math.random().toString(36).slice(2)}`;
      const startToken = `MARKDOWNPASTESTART${nonce}`;
      const endToken = `MARKDOWNPASTEEND${nonce}`;
      if (!range.collapsed) {
        const endRange = document.createRange();
        endRange.setStart(endContainer, range.endOffset);
        endRange.collapse(true);
        endRange.insertNode(document.createTextNode(endToken));
      }
      const startRange = document.createRange();
      startRange.setStart(startContainer, range.startOffset);
      startRange.collapse(true);
      startRange.insertNode(document.createTextNode(startToken));

      let markedMarkdown = this._markdownFromRoot(clone);
      const start = markedMarkdown.indexOf(startToken);
      markedMarkdown = markedMarkdown.replace(startToken, '');
      let end = range.collapsed ? start : markedMarkdown.indexOf(endToken);
      markedMarkdown = markedMarkdown.replace(endToken, '');
      if (start < 0 || end < 0) {
        return { markdown: fallback, start: fallback.length, end: fallback.length };
      }
      return { markdown: markedMarkdown, start, end };
    }

    _markdownFromRoot(root) {
      const markdown = Array.from(root.querySelectorAll(':scope > .block'))
        .map(block => this._blockToMarkdown(block)).filter(Boolean).join('\n\n');
      return markdown ? `${markdown}\n` : '';
    }

    _commit() {
      this._markdown = this._markdownFromRoot(this._canvas);
      const ast = markdownToAST(this._markdown);
      this.dispatchEvent(new CustomEvent('change', {
        bubbles: true,
        detail: { markdown: this._markdown, ast }
      }));
    }

    _blockToMarkdown(block) {
      const type = block.dataset.type;
      const editor = defaultRules[type] && defaultRules[type].editor;
      const content = Array.from(block.children).filter(child => !child.hasAttribute('data-block-control'));
      if (type === 'fencedBox') {
        const marker = block.dataset.marker || '---';
        const label = block.dataset.label || '';
        const container = content[0];
        const childBlocks = container
          ? Array.from(container.querySelectorAll(':scope > .block'))
          : [];
        const body = childBlocks.length
          ? childBlocks.map(child => this._blockToMarkdown(child)).filter(Boolean).join('\n\n')
          : content.map(node => this._elementToMarkdown(node, true)).join('\n');
        return `${marker}${label}\n${body}\n${marker}`;
      }
      if (editor && editor.behavior === 'quote') {
        return content.map(node => this._blockquoteToMarkdown(node, 1, editor.marker || '>')).join('\n');
      }
      return content.map(node => this._elementToMarkdown(node, true)).join('\n');
    }

    _elementToMarkdown(node, blockContext = false) {
      if (node.nodeType === Node.TEXT_NODE) return escapeMarkdown((node.nodeValue || '').replace(/\u200b/g, ''));
      if (node.nodeType !== Node.ELEMENT_NODE) return '';
      if (node.classList.contains('block')) return this._blockToMarkdown(node);
      if (node.hasAttribute('data-block-control')) return '';
      const tag = node.tagName.toLowerCase();
      const inner = () => Array.from(node.childNodes).map(child => this._elementToMarkdown(child)).join('');
      if (/^h[1-6]$/.test(tag)) return `${'#'.repeat(Number(tag[1]))} ${inner().trim()}`;
      if (tag === 'p' || tag === 'div') return this._containerToMarkdown(node).trim();
      if (tag === 'strong' || tag === 'b') return this._wrapInlineMarkdown(inner(), '**');
      if (tag === 'em' || tag === 'i') return this._wrapInlineMarkdown(inner(), '*');
      if (tag === 'u') return this._wrapInlineMarkdown(inner(), '__');
      if (tag === 'span' && node.style.color) {
        const content = inner();
        return content ? `<span style="color:${node.style.color}">${content}</span>` : '';
      }
      if (tag === 'code' && node.parentElement && node.parentElement.tagName === 'PRE') return textOf(node);
      if (tag === 'code') {
        const code = textOf(node);
        return code ? `\`${code.replace(/`/g, '\\`')}\`` : '';
      }
      if (tag === 'a') return `[${inner()}](${this._markdownDestination(node, 'href')})`;
      if (tag === 'img') return `![${node.getAttribute('alt') || ''}](${this._markdownDestination(node, 'src')})`;
      if (tag === 'br') return blockContext ? '' : '\n';
      if (tag === 'pre') return `\`\`\`${node.dataset.language || ''}\n${textOf(node).replace(/\n$/, '')}\n\`\`\``;
      if (tag === 'ul' || tag === 'ol') return this._listToMarkdown(node);
      if (tag === 'li') return Array.from(node.childNodes)
        .filter(child => child.nodeType !== Node.ELEMENT_NODE || !['UL', 'OL'].includes(child.tagName))
        .map(child => this._elementToMarkdown(child)).join('');
      if (tag === 'blockquote') return this._blockquoteToMarkdown(node);
      if (tag === 'table') return Array.from(node.rows).map(row =>
        `| ${Array.from(row.cells).map(cell => this._elementToMarkdown(cell).trim()).join(' | ')} |`).join('\n');
      if (tag === 'tr') return Array.from(node.cells).map(cell => this._elementToMarkdown(cell)).join(' | ');
      if (tag === 'td' || tag === 'th') return inner();
      return inner();
    }

    _markdownDestination(node, attribute) {
      const destination = node.getAttribute(attribute) || '';
      const title = node.getAttribute('title');
      const defaultTitle = node.tagName === 'IMG' ? node.getAttribute('alt') || '' : node.textContent || '';
      return title && title !== defaultTitle
        ? `${destination} "${title.replace(/"/g, '\\"')}"`
        : destination;
    }

    _listToMarkdown(list, depth = 0) {
      const lines = [];
      const ordered = list.tagName === 'OL';
      const type = list.getAttribute('type') || '1';
      const start = list.getAttribute('start') || (type === '1' ? '1' : type);
      Array.from(list.children).filter(item => item.tagName === 'LI').forEach((item, index) => {
        const content = Array.from(item.childNodes)
          .filter(child => child.nodeType !== Node.ELEMENT_NODE || !['UL', 'OL'].includes(child.tagName))
          .map(child => this._elementToMarkdown(child)).join('').trim();
        const indent = item.dataset.indent !== undefined ? item.dataset.indent : '  '.repeat(depth);
        const marker = ordered ? this._orderedMarker(type, start, index) : '-';
        lines.push(`${indent}${marker}${ordered ? '.' : ''} ${content}`);
        Array.from(item.children).filter(child => ['UL', 'OL'].includes(child.tagName)).forEach(nested => {
          const nestedMarkdown = this._listToMarkdown(nested, depth + 1);
          if (nestedMarkdown) lines.push(nestedMarkdown);
        });
      });
      return lines.join('\n');
    }

    _orderedMarker(type, start, index) {
      if (type === '1') return String((Number(start) || 1) + index);
      if (type === 'a' || type === 'A') {
        const value = String.fromCharCode((String(start).toLowerCase().charCodeAt(0) || 97) + index);
        return type === 'A' ? value.toUpperCase() : value;
      }
      return index === 0 ? String(start) : String(start);
    }

    _blockquoteToMarkdown(quote, depth = 1, marker = '>') {
      const lines = [];
      Array.from(quote.childNodes).forEach(child => {
        if (child.nodeType === Node.ELEMENT_NODE && child.tagName === 'BLOCKQUOTE') {
          if (lines.length) lines.push(marker.repeat(depth));
          const nested = this._blockquoteToMarkdown(child, depth + 1, marker);
          if (nested) lines.push(nested);
          return;
        }
        if (child.nodeType === Node.ELEMENT_NODE && child.hasAttribute('data-block-control')) return;
        const content = this._elementToMarkdown(child).trim();
        if (content) {
          if (lines.length) lines.push(marker.repeat(depth));
          content.split('\n').forEach(line => lines.push(`${marker.repeat(depth)} ${line}`));
        }
      });
      return lines.join('\n');
    }

    _wrapInlineMarkdown(content, delimiter) {
      const match = content.match(/^(\s*)([\s\S]*?\S)(\s*)$/);
      if (!match) return content;
      return `${match[1]}${delimiter}${match[2]}${delimiter}${match[3]}`;
    }

    _containerToMarkdown(container) {
      const blockTags = new Set(['ADDRESS', 'BLOCKQUOTE', 'DIV', 'H1', 'H2', 'H3', 'H4', 'H5', 'H6',
        'OL', 'P', 'PRE', 'TABLE', 'UL']);
      let markdown = '';
      let previousWasBlock = false;
      Array.from(container.childNodes).forEach(child => {
        if (child.nodeType === Node.ELEMENT_NODE && child.hasAttribute('data-block-control')) return;
        const isBlock = child.nodeType === Node.ELEMENT_NODE && blockTags.has(child.tagName);
        const content = this._elementToMarkdown(child);
        if (!content && !isBlock) return;
        if (markdown && (isBlock || previousWasBlock) && !markdown.endsWith('\n\n')) markdown += '\n\n';
        markdown += content;
        previousWasBlock = isBlock;
      });
      return markdown;
    }
  }

  if (!global.customElements.get('markdown-wysiwyg-editor')) {
    global.customElements.define('markdown-wysiwyg-editor', MarkdownWysiwygEditor);
  }
  global.MarkdownWysiwygEditor = MarkdownWysiwygEditor;
})(globalThis);
})();
